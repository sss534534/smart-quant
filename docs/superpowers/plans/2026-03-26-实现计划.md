# A股量化交易系统 - 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建完整的A股量化交易系统，包含行情数据、策略引擎、回测、交易、持仓、风控等微服务

**Architecture:** 事件驱动微服务架构，各服务通过Redis消息队列通信，前端Vue3通过Nginx API网关访问各服务

**Tech Stack:** Python 3.10+ / FastAPI / SQLAlchemy / Redis / MySQL / Vue3 / Element Plus / Docker

---

## 文件结构

```
量化投资系统/
├── docker-compose.yml
├── nginx.conf
├── api-gateway/
│   └── nginx.conf
├── services/
│   ├── data-service/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── models.py
│   │   │   └── routers/
│   │   │       └── market.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   ├── strategy-engine/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── models.py
│   │   │   └── routers/
│   │   │       └── strategy.py
│   │   ├── strategies/
│   │   │   └── __init__.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   ├── backtest-service/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── models.py
│   │   │   └── routers/
│   │   │       └── backtest.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   ├── trading-service/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── models.py
│   │   │   └── routers/
│   │   │       └── trading.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   ├── portfolio-service/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── models.py
│   │   │   └── routers/
│   │   │       └── portfolio.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   ├── risk-service/
│   │   ├── app/
│   │   │   ├── __init__.py
│   │   │   ├── main.py
│   │   │   ├── config.py
│   │   │   ├── models.py
│   │   │   └── routers/
│   │   │       └── risk.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   └── common/
│       ├── __init__.py
│       ├── config.py
│       ├── database.py
│       ├── redis_client.py
│       └── message_queue.py
├── web-frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── views/
│   │   ├── components/
│   │   └── router/
│   ├── package.json
│   ├── vite.config.js
│   └── Dockerfile
└── docs/
    └── superpowers/
        ├── specs/
        └── plans/
```

---

## 实现任务

### Task 1: 创建项目基础结构和公共模块

**Files:**
- Create: `E:/workspace/量化投资系统/docker-compose.yml`
- Create: `E:/workspace/量化投资系统/nginx.conf`
- Create: `E:/workspace/量化投资系统/services/common/__init__.py`
- Create: `E:/workspace/量化投资系统/services/common/config.py`
- Create: `E:/workspace/量化投资系统/services/common/database.py`
- Create: `E:/workspace/量化投资系统/services/common/redis_client.py`
- Create: `E:/workspace/量化投资系统/services/common/message_queue.py`

- [ ] **Step 1: 创建 docker-compose.yml**

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: quant123
      MYSQL_DATABASE: quant_system
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api-gateway:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - data-service
      - strategy-engine
      - backtest-service
      - trading-service
      - portfolio-service
      - risk-service

  data-service:
    build: ./services/data-service
    ports:
      - "8006:8006"
    depends_on:
      - mysql
      - redis

  strategy-engine:
    build: ./services/strategy-engine
    ports:
      - "8001:8001"
    depends_on:
      - redis
      - data-service

  backtest-service:
    build: ./services/backtest-service
    ports:
      - "8002:8002"
    depends_on:
      - mysql

  trading-service:
    build: ./services/trading-service
    ports:
      - "8003:8003"
    depends_on:
      - redis

  portfolio-service:
    build: ./services/portfolio-service
    ports:
      - "8004:8004"
    depends_on:
      - mysql

  risk-service:
    build: ./services/risk-service
    ports:
      - "8005:8005"
    depends_on:
      - redis

  web-frontend:
    build: ./web-frontend
    ports:
      - "3000:3000"
    depends_on:
      - api-gateway

volumes:
  mysql_data:
```

- [ ] **Step 2: 创建 nginx.conf**

```nginx
events {
    worker_connections 1024;
}

http {
    upstream data-service {
        server data-service:8006;
    }
    
    upstream strategy-engine {
        server strategy-engine:8001;
    }
    
    upstream backtest-service {
        server backtest-service:8002;
    }
    
    upstream trading-service {
        server trading-service:8003;
    }
    
    upstream portfolio-service {
        server portfolio-service:8004;
    }
    
    upstream risk-service {
        server risk-service:8005;
    }

    server {
        listen 80;
        
        location /api/data/ {
            proxy_pass http://data-service/;
        }
        
        location /api/strategy/ {
            proxy_pass http://strategy-engine/;
        }
        
        location /api/backtest/ {
            proxy_pass http://backtest-service/;
        }
        
        location /api/trading/ {
            proxy_pass http://trading-service/;
        }
        
        location /api/portfolio/ {
            proxy_pass http://portfolio-service/;
        }
        
        location /api/risk/ {
            proxy_pass http://risk-service/;
        }
        
        location / {
            proxy_pass http://web-frontend:3000;
        }
    }
}
```

- [ ] **Step 3: 创建公共模块 config.py**

```python
import os
from typing import Optional

class Config:
    MYSQL_HOST: str = os.getenv("MYSQL_HOST", "mysql")
    MYSQL_PORT: int = int(os.getenv("MYSQL_PORT", "3306"))
    MYSQL_USER: str = os.getenv("MYSQL_USER", "root")
    MYSQL_PASSWORD: str = os.getenv("MYSQL_PASSWORD", "quant123")
    MYSQL_DATABASE: str = os.getenv("MYSQL_DATABASE", "quant_system")
    
    REDIS_HOST: str = os.getenv("REDIS_HOST", "redis")
    REDIS_PORT: int = int(os.getenv("REDIS_PORT", "6379"))
    
    TUSHARE_TOKEN: Optional[str] = os.getenv("TUSHARE_TOKEN")
    
    def get_mysql_url(self) -> str:
        return f"mysql+pymysql://{self.MYSQL_USER}:{self.MYSQL_PASSWORD}@{self.MYSQL_HOST}:{self.MYSQL_PORT}/{self.MYSQL_DATABASE}"

config = Config()
```

- [ ] **Step 4: 创建公共模块 database.py**

```python
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
from .config import config

engine = create_engine(config.get_mysql_url(), pool_pre_ping=True)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 5: 创建公共模块 redis_client.py**

```python
import redis
from .config import config

redis_client = redis.Redis(
    host=config.REDIS_HOST,
    port=config.REDIS_PORT,
    decode_responses=True
)

def get_redis():
    return redis_client
```

- [ ] **Step 6: 创建公共模块 message_queue.py**

```python
import json
from typing import Any, Callable
from .redis_client import redis_client

class MessageQueue:
    def __init__(self, queue_name: str):
        self.queue_name = queue_name
    
    def publish(self, message: dict):
        redis_client.rpush(self.queue_name, json.dumps(message))
    
    def subscribe(self, callback: Callable):
        while True:
            result = redis_client.blpop(self.queue_name, timeout=5)
            if result:
                _, message = result
                callback(json.loads(message))
    
    def get_queue_length(self) -> int:
        return redis_client.llen(self.queue_name)

market_data_queue = MessageQueue("market_data")
trading_signal_queue = MessageQueue("trading_signal")
portfolio_update_queue = MessageQueue("portfolio_update")
```

- [ ] **Step 7: 创建各服务基础文件**

创建 data-service、strategy-engine、backtest-service、trading-service、portfolio-service、risk-service 的基础目录结构和 requirements.txt

---

### Task 2: 数据服务 (data-service)

**Files:**
- Create: `services/data-service/app/main.py`
- Create: `services/data-service/app/config.py`
- Create: `services/data-service/app/models.py`
- Create: `services/data-service/app/routers/market.py`
- Create: `services/data-service/requirements.txt`
- Create: `services/data-service/Dockerfile`

- [ ] **Step 1: 创建 requirements.txt**

```txt
fastapi==0.109.0
uvicorn==0.27.0
sqlalchemy==2.0.25
pymysql==1.1.0
redis==5.0.1
tushare==1.3.7
pydantic==2.5.3
python-dotenv==1.0.0
```

- [ ] **Step 2: 创建 main.py**

```python
from fastapi import FastAPI
from app.routers import market
from common.config import config

app = FastAPI(title="Data Service", version="1.0.0")

app.include_router(market.router, prefix="/market", tags=["market"])

@app.get("/health")
def health_check():
    return {"status": "healthy"}

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8006)
```

- [ ] **Step 3: 创建 routers/market.py**

```python
from fastapi import APIRouter, HTTPException
from typing import List, Optional
from datetime import datetime
from pydantic import BaseModel

router = APIRouter()

class StockQuote(BaseModel):
    code: str
    name: str
    price: float
    change: float
    change_pct: float
    volume: int
    amount: float
    timestamp: datetime

class KLine(BaseModel):
    code: str
    date: str
    open: float
    high: float
    low: float
    close: float
    volume: int

@router.get("/quote/{stock_code}", response_model=StockQuote)
async def get_quote(stock_code: str):
    return {
        "code": stock_code,
        "name": "股票名称",
        "price": 100.0,
        "change": 1.5,
        "change_pct": 1.52,
        "volume": 1000000,
        "amount": 100000000.0,
        "timestamp": datetime.now()
    }

@router.get("/kline/{stock_code}", response_model=List[KLine])
async def get_kline(stock_code: str, start_date: str, end_date: str):
    return []
```

- [ ] **Step 4: 创建 Dockerfile**

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8006"]
```

---

### Task 3: 策略引擎 (strategy-engine)

**Files:**
- Create: `services/strategy-engine/app/main.py`
- Create: `services/strategy-engine/app/config.py`
- Create: `services/strategy-engine/app/models.py`
- Create: `services/strategy-engine/app/routers/strategy.py`
- Create: `services/strategy-engine/strategies/__init__.py`
- Create: `services/strategy-engine/strategies/base.py`
- Create: `services/strategy-engine/strategies/indicators.py`
- Create: `services/strategy-engine/requirements.txt`
- Create: `services/strategy-engine/Dockerfile`

- [ ] **Step 1: 创建 requirements.txt**

```txt
fastapi==0.109.0
uvicorn==0.27.0
numpy==1.26.3
pandas==2.1.4
pydantic==2.5.3
```

- [ ] **Step 2: 创建 strategies/base.py**

```python
from abc import ABC, abstractmethod
from typing import Dict, List, Optional
from pandas import DataFrame

class BaseStrategy(ABC):
    name: str = "base_strategy"
    
    @abstractmethod
    def initialize(self, params: Dict):
        pass
    
    @abstractmethod
    def on_bar(self, bar: Dict) -> Optional[Dict]:
        pass
    
    def on_trade(self, trade: Dict):
        pass
```

- [ ] **Step 3: 创建 strategies/indicators.py**

```python
import pandas as pd
import numpy as np

def SMA(close: pd.Series, period: int) -> pd.Series:
    return close.rolling(window=period).mean()

def EMA(close: pd.Series, period: int) -> pd.Series:
    return close.ewm(span=period, adjust=False).mean()

def MACD(close: pd.Series, fast: int = 12, slow: int = 26, signal: int = 9):
    ema_fast = EMA(close, fast)
    ema_slow = EMA(close, slow)
    dif = ema_fast - ema_slow
    dea = EMA(dif, signal)
    macd = (dif - dea) * 2
    return dif, dea, macd

def RSI(close: pd.Series, period: int = 14) -> pd.Series:
    delta = close.diff()
    gain = (delta.where(delta > 0, 0)).rolling(window=period).mean()
    loss = (-delta.where(delta < 0, 0)).rolling(window=period).mean()
    rs = gain / loss
    rsi = 100 - (100 / (1 + rs))
    return rsi

def KDJ(high: pd.Series, low: pd.Series, close: pd.Series, period: int = 9):
    lowest_low = low.rolling(window=period).min()
    highest_high = high.rolling(window=period).max()
    rsv = (close - lowest_low) / (highest_high - lowest_low) * 100
    k = rsv.ewm(com=2, adjust=False).mean()
    d = k.ewm(com=2, adjust=False).mean()
    j = 3 * k - 2 * d
    return k, d, j

def BOLL(close: pd.Series, period: int = 20, std: int = 2):
    sma = close.rolling(window=period).mean()
    std_dev = close.rolling(window=period).std()
    upper = sma + std_dev * std
    lower = sma - std_dev * std
    return upper, sma, lower
```

- [ ] **Step 4: 创建 routers/strategy.py**

```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from typing import Dict, List, Optional

router = APIRouter()

class StrategySignal(BaseModel):
    code: str
    action: str
    price: float
    quantity: int
    reason: str

class StrategyParams(BaseModel):
    name: str
    params: Dict

@router.post("/run")
async def run_strategy(params: StrategyParams):
    return {"status": "running", "strategy_id": "xxx"}

@router.get("/signals")
async def get_signals(strategy_id: str):
    return []
```

- [ ] **Step 5: 创建 main.py 和 Dockerfile**

参考 data-service 格式

---

### Task 4: 回测服务 (backtest-service)

**Files:**
- Create: `services/backtest-service/app/main.py`
- Create: `services/backtest-service/app/models.py`
- Create: `services/backtest-service/app/routers/backtest.py`
- Create: `services/backtest-service/requirements.txt`
- Create: `services/backtest-service/Dockerfile`

- [ ] **Step 1: 创建 routers/backtest.py**

```python
from fastapi import APIRouter
from pydantic import BaseModel
from typing import List, Dict
from datetime import datetime

router = APIRouter()

class BacktestRequest(BaseModel):
    strategy_id: str
    start_date: str
    end_date: str
    initial_capital: float

class BacktestResult(BaseModel):
    total_return: float
    annual_return: float
    sharpe_ratio: float
    max_drawdown: float
    win_rate: float
    trades: List[Dict]

@router.post("/run", response_model=BacktestResult)
async def run_backtest(request: BacktestRequest):
    return {
        "total_return": 0.15,
        "annual_return": 0.12,
        "sharpe_ratio": 1.5,
        "max_drawdown": 0.08,
        "win_rate": 0.6,
        "trades": []
    }

@router.get("/result/{backtest_id}")
async def get_result(backtest_id: str):
    return {}
```

---

### Task 5: 交易服务 (trading-service)

**Files:**
- Create: `services/trading-service/app/main.py`
- Create: `services/trading-service/app/models.py`
- Create: `services/trading-service/app/routers/trading.py`
- Create: `services/trading-service/requirements.txt`
- Create: `services/trading-service/Dockerfile`

- [ ] **Step 1: 创建 routers/trading.py**

```python
from fastapi import APIRouter
from pydantic import BaseModel
from typing import List
from datetime import datetime

router = APIRouter()

class Order(BaseModel):
    order_id: str
    code: str
    direction: str
    price: float
    quantity: int
    status: str

class TradeRequest(BaseModel):
    code: str
    direction: str
    price: float
    quantity: int

@router.post("/order", response_model=Order)
async def create_order(request: TradeRequest):
    return {
        "order_id": "xxx",
        "code": request.code,
        "direction": request.direction,
        "price": request.price,
        "quantity": request.quantity,
        "status": "pending"
    }

@router.get("/orders")
async def get_orders():
    return []

@router.get("/status")
async def get_status():
    return {"simulated": True}
```

---

### Task 6: 持仓服务 (portfolio-service)

**Files:**
- Create: `services/portfolio-service/app/main.py`
- Create: `services/portfolio-service/app/models.py`
- Create: `services/portfolio-service/app/routers/portfolio.py`
- Create: `services/portfolio-service/requirements.txt`
- Create: `services/portfolio-service/Dockerfile`

- [ ] **Step 1: 创建 routers/portfolio.py**

```python
from fastapi import APIRouter
from pydantic import BaseModel
from typing import List
from datetime import datetime

router = APIRouter()

class Position(BaseModel):
    code: str
    name: str
    quantity: int
    cost: float
    current_price: float
    profit: float
    profit_pct: float

class AccountSummary(BaseModel):
    total_assets: float
    cash: float
    positions_value: float
    total_profit: float

@router.get("/positions", response_model=List[Position])
async def get_positions():
    return []

@router.get("/summary", response_model=AccountSummary)
async def get_summary():
    return {
        "total_assets": 100000.0,
        "cash": 50000.0,
        "positions_value": 50000.0,
        "total_profit": 0.0
    }
```

---

### Task 7: 风控服务 (risk-service)

**Files:**
- Create: `services/risk-service/app/main.py`
- Create: `services/risk-service/app/models.py`
- Create: `services/risk-service/app/routers/risk.py`
- Create: `services/risk-service/requirements.txt`
- Create: `services/risk-service/Dockerfile`

- [ ] **Step 1: 创建 routers/risk.py**

```python
from fastapi import APIRouter
from pydantic import BaseModel
from typing import Dict

router = APIRouter()

class RiskCheckRequest(BaseModel):
    code: str
    direction: str
    price: float
    quantity: int

class RiskCheckResult(BaseModel):
    passed: bool
    message: str

@router.post("/check", response_model=RiskCheckResult)
async def check_risk(request: RiskCheckRequest):
    return {
        "passed": True,
        "message": "风控检查通过"
    }

@router.get("/limits")
async def get_limits():
    return {
        "max_position_pct": 0.2,
        "max_single_trade": 100000,
        "stop_loss_pct": 0.05
    }
```

---

### Task 8: 前端界面 (web-frontend)

**Files:**
- Create: `web-frontend/package.json`
- Create: `web-frontend/vite.config.js`
- Create: `web-frontend/index.html`
- Create: `web-frontend/src/main.js`
- Create: `web-frontend/src/App.vue`
- Create: `web-frontend/src/router/index.js`
- Create: `web-frontend/src/api/index.js`
- Create: `web-frontend/src/views/Dashboard.vue`
- Create: `web-frontend/src/views/Strategy.vue`
- Create: `web-frontend/src/views/Backtest.vue`
- Create: `web-frontend/src/views/Trading.vue`
- Create: `web-frontend/src/views/Portfolio.vue`
- Create: `web-frontend/Dockerfile`

- [ ] **Step 1: 创建 package.json**

```json
{
  "name": "quant-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.15",
    "vue-router": "^4.2.5",
    "axios": "^1.6.5",
    "element-plus": "^2.5.1",
    "echarts": "^5.4.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.3",
    "vite": "^5.0.11"
  }
}
```

- [ ] **Step 2: 创建 vite.config.js**

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    host: '0.0.0.0',
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://api-gateway',
        changeOrigin: true
      }
    }
  }
})
```

- [ ] **Step 3: 创建 main.js**

```javascript
import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(ElementPlus)
app.use(router)
app.mount('#app')
```

- [ ] **Step 4: 创建 App.vue**

```vue
<template>
  <div id="app">
    <el-container>
      <el-header>
        <h1>量化投资系统</h1>
        <el-menu mode="horizontal" :router="true">
          <el-menu-item index="/">Dashboard</el-menu-item>
          <el-menu-item index="/strategy">策略</el-menu-item>
          <el-menu-item index="/backtest">回测</el-menu-item>
          <el-menu-item index="/trading">交易</el-menu-item>
          <el-menu-item index="/portfolio">持仓</el-menu-item>
        </el-menu>
      </el-header>
      <el-main>
        <router-view />
      </el-main>
    </el-container>
  </div>
</template>

<script setup>
</script>

<style>
#app {
  height: 100vh;
}
.el-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
</style>
```

- [ ] **Step 5: 创建 router/index.js**

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Dashboard from '../views/Dashboard.vue'
import Strategy from '../views/Strategy.vue'
import Backtest from '../views/Backtest.vue'
import Trading from '../views/Trading.vue'
import Portfolio from '../views/Portfolio.vue'

const routes = [
  { path: '/', component: Dashboard },
  { path: '/strategy', component: Strategy },
  { path: '/backtest', component: Backtest },
  { path: '/trading', component: Trading },
  { path: '/portfolio', component: Portfolio }
]

export default createRouter({
  history: createWebHistory(),
  routes
})
```

- [ ] **Step 6: 创建 Dashboard.vue**

```vue
<template>
  <div>
    <el-row :gutter="20">
      <el-col :span="6">
        <el-card>
          <template #header>总资产</template>
          <div class="stat-value">¥ {{ accountSummary.total_assets.toLocaleString() }}</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <template #header>持仓市值</template>
          <div class="stat-value">¥ {{ accountSummary.positions_value.toLocaleString() }}</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <template #header>可用资金</template>
          <div class="stat-value">¥ {{ accountSummary.cash.toLocaleString() }}</div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <template #header>总收益</template>
          <div class="stat-value" :class="{ 'profit': accountSummary.total_profit > 0 }">
            ¥ {{ accountSummary.total_profit.toLocaleString() }}
          </div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import axios from 'axios'

const accountSummary = ref({
  total_assets: 0,
  positions_value: 0,
  cash: 0,
  total_profit: 0
})

onMounted(async () => {
  const res = await axios.get('/api/portfolio/summary')
  accountSummary.value = res.data
})
</script>

<style scoped>
.stat-value {
  font-size: 24px;
  font-weight: bold;
}
.profit {
  color: #67c23a;
}
</style>
```

- [ ] **Step 7: 创建其他视图和 Dockerfile**

创建 Strategy.vue、Backtest.vue、Trading.vue、Portfolio.vue 基础页面结构

---

## 计划完成

计划已保存到 `docs/superpowers/plans/2026-03-26-实现计划.md`

**两个执行选项：**

1. **Subagent-Driven (推荐)** - 我为每个任务分配一个子代理，任务间进行审查，快速迭代

2. **Inline Execution** - 在当前会话中执行任务，使用 executing-plans 进行批量执行和检查点

**选择哪种方式？**
