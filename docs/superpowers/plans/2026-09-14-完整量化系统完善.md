# 完整量化系统完善 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让现有 A 股量化系统 6 个服务真正跑起来，打通「行情 → 策略 → 信号 → 订单 → 风控 → 持仓」完整闭环，并把交易/持仓/风控持久化落库，用关键链路测试 + docker-compose 手工 E2E 验证，最后完善前端体验。

**Architecture:** 同步 HTTP 桥（方案 A）：data-service(mock provider) 提供行情 → strategy-engine 用一个 `feed_bars` 定时任务把 quote 转成 BarData 喂给运行中的策略 → 策略产生 StrategySignal → 信号回调持久化 `strategy_signals` 并 POST trading-service 下单 → trading 调 risk-service 风控(改可选认证) 后模拟成交 → 成交同步 portfolio-service 更新持仓与资金。引擎内存是快路径，DB 是持久真相：各服务 lifespan 幂等 `common.init_db()` 建表，路由/引擎写点双写 DB，启动时从 DB 重建引擎。

**Tech Stack:** FastAPI 0.109 / uvicorn 0.27 / SQLAlchemy 2.0.25(M+SQLite 测试) / pydantic 2.5.3 + pydantic-settings / httpx 0.26 / APScheduler(AsyncIOScheduler) / pytest 7.4.4 + pytest-asyncio / Vue3 + Element Plus + ECharts / Docker Compose。

## Global Constraints

- Python 3.10(CI)/3.11(本地)，全部命令以 Windows PowerShell 运行；测试命令统一 `py -m pytest tests/ -v`（项目根目录）。
- 依赖版本收敛基线：`fastapi==0.109.0`、`uvicorn==0.27.0`、`pydantic==2.5.3`、`pydantic-settings==2.1.0`、`sqlalchemy==2.0.25`、`pymysql==1.1.0`、`redis==5.0.1`、`python-dotenv==1.0.0`、`httpx==0.26.0`、`psutil==5.9.8`、`prometheus-fastapi-instrumentator==6.1.0`；按服务补 `apscheduler==3.10.4`、`numpy==1.26.3`、`pandas==2.1.4`、`tushare==1.3.7`。
- 所有服务统一从 `common` 导入 `Base/get_db/SessionLocal/init_db`；模型统一用 `common.models.*`；删除所有服务内的「独立 Base」。
- `SECURITY_SECRET_KEY` 默认值 `dev-secret-key-quant-system-2026`（正好 32 字符，通过 ≥32 校验）。
- `SECRET_KEY` 校验规则不动；`AuthService.initialize` 必须幂等（同一进程被多个 TestClient lifespan 反复调用也不报错）。
- 写请求依赖加 `AuthUser` 认证；**内部 HTTP 调用**（trading→risk、strategy→trading、trading→portfolio）使用 `get_optional_user`，不强制 token。
- 不引入新依赖框架；不修改既有 OpenAPI 路由路径；`/api/v1/*` 与 `/api/*` 双前缀注册保持原样。
- 非 git 仓库：每个 Task 的 commit 步骤写入说明但不强制执行；如需版本管理可 `git init` 后按步骤提交。
- 本计划不包含：prod compose 缺失文件、SSL、grafana/prometheus 配置（均为后续项）。

---

## R1 稳定运行修复

### Task 1: 修复 SECRET_KEY 默认值使 6 服务可导入

**Files:**

- Modify: `services/common/config.py:142`
- Modify: `.env.example:57`

**Interfaces:**

- Consumes: 无
- Produces: `settings.security.SECRET_KEY`（≥32 字符），`AuthService.initialize(secret_key=...)` 可正常签名 token

**背景：** `SecurityConfig.SECRET_KEY` 默认值是 `your-secret-key-here`（21 字符 < 32）触发 field_validator 抛错；且 `Settings` 无 `SECRET_KEY` 顶层字段，各服务 `main.py` 里 `settings.SECRET_KEY` 实际上是 Pydantic 的额外字段透传（来自 env `SECRET_KEY`，但 env_prefix=`SECURITY_` 不匹配），导致默认导入失败。

- [ ] **Step 1: 写失败测试（导入冒烟）**

```python
# tests/test_boot.py
def test_settings_secret_key_valid():
    from common.config import settings
    assert len(settings.security.SECRET_KEY) >= 32

def test_auth_init_idempotent():
    import asyncio
    from common.auth import auth_service
    async def run():
        await auth_service.initialize(secret_key="dev-secret-key-quant-system-2026")
        before = len(auth_service._token_manager._tokens) if auth_service._token_manager else 0
        await auth_service.initialize(secret_key="dev-secret-key-quant-system-2026")
        after = len(auth_service._token_manager._tokens) if auth_service._token_manager else 0
        assert before == after
    asyncio.run(run())
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_boot.py -v`（先写 `services/common/requirements.txt` 可用，见 R6 Task 19 后再跑通；当前本地环境已装依赖可用 `py -m pytest`）
Expected: FAIL —— `ValueError: 密钥长度不能少于32位` 或 import error。

- [ ] **Step 3: 修改默认值与幂等性**

修改 `services/common/config.py` 第 142 行：

```python
    SECRET_KEY: str = Field("dev-secret-key-quant-system-2026", description="密钥")
```

修改 `services/common/auth.py` 的 `AuthService.initialize`（第 311 行起）：

```python
    async def initialize(self, secret_key: str):
        if self._token_manager is not None:
            logger.info("Auth service already initialized, skip")
            return
        self._token_manager = TokenManager(secret_key)
        await self.create_user(
            username="admin",
            password="admin123",
            email="admin@quant.com",
            role=UserRole.ADMIN,
        )
        logger.info("Auth service initialized")
```

修改 `.env.example` 第 56-59 行：

```dotenv
# 安全配置
SECURITY_SECRET_KEY=dev-secret-key-quant-system-2026
SECURITY_ACCESS_TOKEN_EXPIRE_MINUTES=30
SECURITY_REFRESH_TOKEN_EXPIRE_DAYS=7
SECURITY_ALLOWED_HOSTS=*
SECURITY_CORS_ORIGINS=*
```

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_boot.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 提交**

```bash
git status   # 若为 git 仓库则提交；本项目非 git，记录说明文件变更即可
git add services/common/config.py services/common/auth.py .env.example tests/test_boot.py
git commit -m "fix(common): use 32-char dev SECRET_KEY and idempotent auth init"
```

---

### Task 2: 修复 strategy-engine 导入缺陷（manager 引用不存在的模块 + 缺全局实例）

**Files:**

- Create: `services/strategy-engine/app/engine/__init__.py`
- Modify: `services/strategy-engine/app/engine/manager.py:5,14,42-60`
- Test: `tests/test_boot.py`

**Interfaces:**

- Consumes: `strategies.base` 里的 `BaseStrategy/StrategyStatus/StrategySignal/SignalAction/BarData`
- Produces: 模块级单例 `strategy_manager: StrategyManager`，`StrategyManager.run(strategy_id, bar_data: dict)` 接收 dict

**背景：** `app/engine/` 目录没有 `__init__.py` 也没有 `strategy.py`，但 `manager.py:5` 写了 `from .strategy import ...` ⇒ 首次调用 `/run` 会 `ModuleNotFoundError`；并且 `manager.py` 末尾没有定义 `strategy_manager`，而 `routers/strategy.py:207` 却 `from app.engine.manager import strategy_manager` ⇒ 同样报错。

- [ ] **Step 1: 写失败测试（直接导入 manager）**

```python
# 追加到 tests/test_boot.py
def test_strategy_manager_importable():
    import sys
    sys.path.insert(0, "services/strategy-engine")
    from app.engine.manager import strategy_manager
    assert strategy_manager is not None
    assert hasattr(strategy_manager, "register_signal_callback")
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_boot.py::test_strategy_manager_importable -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.engine.strategy'` 或 `ImportError`

- [ ] **Step 3: 修复**

创建 `services/strategy-engine/app/engine/__init__.py`（空文件）。

修改 `services/strategy-engine/app/engine/manager.py`：

- 第 5 行改为：

```python
from strategies.base import BaseStrategy, StrategyStatus, StrategySignal, SignalAction, BarData
```

- 第 14 行后（`self._initialized` 后）不需要改动。
- 文件末尾（第 231 行后）追加：

```python


# 全局策略管理器实例
strategy_manager = StrategyManager()
```

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_boot.py::test_strategy_manager_importable -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add services/strategy-engine/app/engine/__init__.py services/strategy-engine/app/engine/manager.py tests/test_boot.py
git commit -m "fix(strategy-engine): import from strategies.base, add manager singleton"
```

---

### Task 3: data-service 收敛到 common 模型并修复独立 Base 导致的导入错

**Files:**

- Modify: `services/data-service/app/routers/market.py:21,32`
- Modify: `services/data-service/app/main.py:10,32`
- Delete: `services/data-service/app/database.py`
- Delete: `services/data-service/app/models/market.py`
- Test: `tests/test_boot.py`

**Interfaces:**

- Consumes: `common.models.market.MarketData/StockInfo/Calendar`（这些模型属 `common` 的唯一 Base）；`settings.tushare.TUSHARE_TOKEN`
- Produces: `from common import init_db` 幂等建表；`app/main.py` 不再有独立 `init_db`

**背景：** `app/models/market.py` 缺 `Boolean` 导入（`is_holiday = Column(Boolean,...)` 处 `NameError: name 'Boolean' is not defined`）；且它注册在 data-service 自己的 Base（`app/database.py`）上，与 `common` Base 分叉。删除这两个文件、改用 `common.models.market`。

- [ ] **Step 1: 写失败测试**

```python
# 追加到 tests/test_boot.py
def test_data_service_app_importable():
    import sys
    sys.path.insert(0, "services/data-service")
    from app.main import app
    assert app.title == "Data Service"
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_boot.py::test_data_service_app_importable -v`
Expected: FAIL —— `NameError: name 'Boolean' is not defined`（在导入 `app.routers.market` 时触发）。

- [ ] **Step 3: 修改路由**

修改 `services/data-service/app/routers/market.py`：

- 第 21 行改为：`from common.models.market import MarketData, StockInfo, Calendar`
- 第 30-33 行改为：

```python
from common import settings


# 数据提供者
tushare_provider = TushareDataProvider(
    token=settings.tushare.TUSHARE_TOKEN or ""  # 从配置加载
)
mock_provider = MockDataProvider()
```

- [ ] **Step 4: 修改 main.py 并用 grep 确认无其他引用后删除文件**

修改 `services/data-service/app/main.py` 第 10 行：`from app.database import init_db` → 删除该行；第 32 行 `init_db()` 前加入 `from common import init_db`（或在顶部 from common import 列表中加 `init_db`）。

```python
from common import (
    setup_logging,
    settings,
    init_db,
    RequestMiddleware,
    ...
)
```

执行并确认：

```powershell
rg -n "app\.database|app\.models\.market" services/data-service
```

Expected: 无输出（除被修改的文件已不存在引用）。

删除：

```powershell
Remove-Item services/data-service/app/database.py
Remove-Item services/data-service/app/models/market.py
```

> 保留 `app/models/__init__.py`、`app/__init__.py`、`app/utils/cache.py`（确认无引用后也可删除，但不强制）。

- [ ] **Step 5: 运行确认通过**

Run: `py -m pytest tests/test_boot.py::test_data_service_app_importable -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add services/data-service/app/routers/market.py services/data-service/app/main.py -u
git add -A services/data-service/app/database.py services/data-service/app/models/market.py
git commit -m "fix(data-service): unify models into common, drop broken local Base"
```

---

### Task 4: 各服务 lifespan 统一 init_db 建表 + 冒烟测试

**Files:**

- Modify: `services/strategy-engine/app/main.py:29-37`
- Modify: `services/backtest-service/app/main.py:27-33`
- Modify: `services/trading-service/app/main.py:27-33`
- Modify: `services/portfolio-service/app/main.py:27-36`
- Modify: `services/risk-service/app/main.py:27-37`
- Test: `tests/test_boot.py`

**Interfaces:**

- Consumes: `common.init_db()`
- Produces: 任意服务启动即在 MySQL 建齐 `orders/trades/positions/accounts/account_logs/strategies/strategy_signals/risk_limits/risk_checks/backtests/...` 所有表（幂等）。

**背景：** 当前只有 data-service 调 init_db；其余服务若要持久化必须先建表。用 `common.init_db()`（对 `common` Base 的 `create_all`），对各服务 lifespan 在 `auth_service.initialize(...)` 前后 insert 一行。

- [ ] **Step 1: 写失败测试（所有服务 app 均可导入之外，init_db 建表到 sqlite）**

```python
# 追加到 tests/test_boot.py
def test_init_db_creates_all_tables():
    from sqlalchemy import create_engine, inspect
    from sqlalchemy.pool import StaticPool
    from common import Base
    engine = create_engine(
        "sqlite://",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(bind=engine)
    names = sorted(inspect(engine).get_table_names())
    for expected in ("orders", "positions", "accounts", "strategy_signals", "risk_limits", "risk_checks", "backtests"):
        assert expected in names, names
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_boot.py::test_init_db_creates_all_tables -v`
Expected: FAIL（common 的 Base.metadata 未注册这些模型的表，因为测试没先导入 `common.models`）——若先导入则直接 PASS；无需强制先失败，属「模型注册验证」。

- [ ] **Step 3: 复制一份 test 文件顶部确保导入模型**

把上面测试改为在断言前显式导入：

```python
def test_init_db_creates_all_tables():
    from common import models  # noqa: F401  注册所有表
    from sqlalchemy import create_engine, inspect
    from sqlalchemy.pool import StaticPool
    from common import Base
    engine = create_engine(
        "sqlite://",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(bind=engine)
    names = sorted(inspect(engine).get_table_names())
    for expected in ("orders", "positions", "accounts", "strategy_signals", "risk_limits", "risk_checks", "backtests"):
        assert expected in names, names
```

- [ ] **Step 4: 为 5 个服务 lifespan 加 init_db**

`services/strategy-engine/app/main.py` lifespan 改为：

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    logger.info("Strategy Engine starting up...")
    from common import init_db, auth_service, scheduler
    init_db()
    await auth_service.initialize(secret_key=settings.SECRET_KEY)
    scheduler.start()
    yield
    scheduler.shutdown()
    logger.info("Strategy Engine shutting down...")
```

`services/backtest-service/app/main.py`、`services/trading-service/app/main.py` lifespan 改为（backtest 也需 `init_db()`）：

```python
    from common import init_db
    init_db()
    await auth_service.initialize(secret_key=settings.SECRET_KEY)
```

`services/risk-service/app/main.py` lifespan 改为（保留 scheduler 启动上下文）：

```python
    from common import init_db, auth_service, scheduler
    init_db()
    await auth_service.initialize(secret_key=settings.SECRET_KEY)
    scheduler.start()
```

`services/portfolio-service/app/main.py` lifespan 改为：

```python
    from common import init_db
    init_db()
    from app.engine import portfolio_engine
    await portfolio_engine.initialize(initial_capital=100000.0)
    await auth_service.initialize(secret_key=settings.SECRET_KEY)
```

- [ ] **Step 5: 全量冒烟**

Run: `py -m pytest tests/test_boot.py -v`
Expected: PASS（至少包含 4 个 test）

Run: `py -m py_compile services/strategy-engine/app/main.py services/backtest-service/app/main.py services/trading-service/app/main.py services/portfolio-service/app/main.py services/risk-service/app/main.py services/data-service/app/main.py`
Expected: 无输出（编译通过）。

- [ ] **Step 6: 提交**

```bash
git add services/*/app/main.py tests/test_boot.py
git commit -m "feat(common): idempotent init_db in every service lifespan"
```

---

### Task 5: risk `/check` 改可选认证，打通内部风控调用

**Files:**

- Modify: `services/risk-service/app/routers/risk.py:10-23,40`
- Test: `tests/test_boot.py`

**Interfaces:**

- Consumes: `common.get_optional_user`（已存在，`common.auth_dependency.get_optional_user`）
- Produces: `POST /risk/check` 无 token 返回 200（而非 401）；带 token 返回 200

**背景：** trading-engine 下单时以无 token 调 `http://risk-service:8005/risk/check`，被 `get_current_user` 401 截断 ⇒ 风控形同虚设（降级放行）。

- [ ] **Step 1: 写失败测试**

```python
# 追加到 tests/test_boot.py
def test_risk_check_allows_no_token():
    import sys
    sys.path.insert(0, "services/risk-service")
    from fastapi.testclient import TestClient
    from app.main import app
    with TestClient(app) as client:
        resp = client.post("/risk/check", json={
            "order_no": "O-TEST-001",
            "code": "600000",
            "direction": "buy",
            "price": 10.0,
            "quantity": 100,
        })
    assert resp.status_code == 200
    assert "passed" in resp.json()
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_boot.py::test_risk_check_allows_no_token -v`
Expected: FAIL —— 401（TestClient with lifespan 会走 auth_service.initialize；若上一任务幂等已生效且进程共享 auth_service，仍因无 token 401）。

- [ ] **Step 3: 修改依赖**

修改 `services/risk-service/app/routers/risk.py`：

- 第 20-22 行，把 `get_current_user`, `AuthUser` 的导入改为加入：

```python
    get_optional_user,
```

- 第 40 行签名改为：

```python
    current_user: Optional[AuthUser] = Depends(get_optional_user),
```

（`Optional` 已在文件顶部第 7 行导入。）

- 其余写端点（`create_limit/update_limit/delete_limit`）保持 `get_current_user` 不变。

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_boot.py::test_risk_check_allows_no_token -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add services/risk-service/app/routers/risk.py tests/test_boot.py
git commit -m "fix(risk): /check uses optional auth so internal calls pass risk control"
```

---

## R2 打通闭环：行情 → 策略 → 信号 → 订单

### Task 6: 修复策略管理器的信号链路（dict→BarData + 真正触发 callbacks + 去重）

**Files:**

- Modify: `services/strategy-engine/app/engine/manager.py:42-77,195-205`
- Test: `tests/test_manager.py`

**Interfaces:**

- Consumes: `BarData`(strategies.base)；`StrategyManager.register_signal_callback(cb)`
- Produces: `process_bars(bar_data: dict)` 只做一次直接处理（不再入队避免重复）；`run()` 内部把 dict 转 `BarData` 再喂策略；策略产生 signal 后调用 `strategy.emit_signal` **且** `self.emit_signals(signal)`（触发所有注册回调）

**背景：** ① `run()` 直接 `strategy.on_bar(bar_data)` 传 dict，但 `DualMAStrategy.on_bar` 访问 `bar.close`/`bar.code` ⇒ AttributeError；② `manager.emit_signals()` 存在但从没被调用 ⇒ 信号回调永远不会触发；③ `process_bars` 既入队又直连处理，且每个运行策略的 run_loop 也从同一队列消费同一 bar ⇒ 每个 bar 会重复喂入。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_manager.py
import asyncio
import pytest
import sys
sys.path.insert(0, "services/strategy-engine")

from app.engine.manager import StrategyManager
from strategies.base import BarData, SignalAction, StrategySignal


class OneShotStrategy:
    """最小测试策略：第一根 bar 就发出 BUY 信号"""
    _status = ...  # placeholder

# 用继承基类的方式写最小策略
from strategies.base import BaseStrategy

class OneShotStrategy(BaseStrategy):
    name = "oneshot"
    version = "1.0.0"
    description = "emit buy on first bar"
    def initialize(self, params: dict):
        self._initialized = True
    def on_bar(self, bar: BarData):
        # 用 BarData 校验：若收到 dict 则 bar['code'] 抛 TypeError
        assert isinstance(bar, BarData), f"expected BarData, got {type(bar)}"
        if len(self.get_bar_history(bar.code)) == 1:
            return self.create_signal(bar.code, SignalAction.BUY, bar.close, 100, "test")
        return None


@pytest.mark.asyncio
async def test_run_converts_dict_and_fires_callbacks():
    manager = StrategyManager()
    await manager.initialize()

    events = []
    manager.register_signal_callback(lambda s: events.append(s))

    strat = OneShotStrategy("S1", {"code": "600000"})
    strat.initialize({"code": "600000"})
    manager.add_strategy(strat)

    bar = BarData(code="600000", timestamp="2026-09-14T10:00:00",
                  open=10.0, high=10.5, low=9.8, close=10.2, volume=100000)

    # 直接处理模式：一次 process_bars 只喂一次
    for _ in range(3):
        await manager.process_bars(bar.to_dict())

    emitted = [s for s in events if s is not None]
    assert len(emitted) == 1, f"expected exactly 1 signal, got {len(emitted)}"
    sig = emitted[0]
    assert isinstance(sig, StrategySignal)
    assert sig.action == SignalAction.BUY
    assert sig.strategy_id == "S1"
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_manager.py -v`
Expected: FAIL —— `TypeError: 'str' object is not subscriptable` 或 `BarData` 断言失败 / 信号数量 ≠ 1（重复入队）。

- [ ] **Step 3: 修改 manager.py**

在 `manager.py` 顶部（BarData 已由 Task 2 引入）新增 dict→BarData 转换：

```python
def bar_from_dict(data: dict) -> BarData:
    ts = data["timestamp"]
    if isinstance(ts, str):
        ts = datetime.fromisoformat(ts.replace("Z", "+00:00"))
    return BarData(
        code=data["code"],
        timestamp=ts,
        open=float(data.get("open", 0)),
        high=float(data.get("high", 0)),
        low=float(data.get("low", 0)),
        close=float(data.get("close", 0)),
        volume=int(data.get("volume", 0)),
        amount=float(data.get("amount", 0)),
        turnover=float(data.get("turnover", 0)),
    )
```

`manager.run`（第 42-60 行）改为：

```python
    async def run(self, strategy_id: str, bar_data: Dict):
        """运行单个策略"""
        strategy = self._strategies.get(strategy_id)
        if not strategy:
            logger.error(f"Strategy not found: {strategy_id}")
            return

        try:
            bar = bar_from_dict(bar_data)
            signal = await asyncio.get_event_loop().run_in_executor(
                None,
                lambda: strategy.on_bar(bar)
            )

            if signal:
                strategy.emit_signal(signal)
                # 通知所有注册的信号回调（落库 / 下单）
                self.emit_signals(signal)

        except Exception as e:
            logger.error(f"Error processing bar for strategy {strategy_id}: {e}")
            strategy.notify("on_error", {"strategy_id": strategy_id, "error": str(e)})
```

`process_bars`（第 62-77 行）改为去掉「入队 + run_loop 双消费」：

```python
    async def process_bars(self, bar_data: Dict):
        """处理 K 线数据，发送给所有运行中的策略（直接处理，单次喂入）"""
        if not self._initialized:
            await self.initialize()

        tasks = [
            self._process_single_strategy(strategy_id, bar_data)
            for strategy_id, strategy in self._strategies.items()
            if strategy._status == StrategyStatus.RUNNING
        ]

        await asyncio.gather(*tasks, return_exceptions=True)
```

> 说明：`run_loop`（第 216-231 行）保留但不再被喂数据（队列不再入队），为空闲守卫，不影响正确性。

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_manager.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: 提交**

```bash
git add services/strategy-engine/app/engine/manager.py tests/test_manager.py
git commit -m "fix(strategy-engine): feed BarData not dict, emit signals to callbacks once"
```

---

### Task 7: feed_bars 定时任务（data-service 行情 → BarData → 策略）

**Files:**

- Create: `services/strategy-engine/app/feed.py`
- Modify: `services/strategy-engine/app/main.py:29-37`
- Test: `tests/test_feed.py`

**Interfaces:**

- Consumes: `app.data_client.data_client.get_quote / get_stock_list`；`app.engine.manager.strategy_manager`
- Produces: `async def feed_market_bars()` —— 对每个运行中策略的 `params["code"]` 拉 quote，转 `BarData.to_dict()` 后 `await strategy_manager.process_bars(...)`

**背景：** strategy-engine lifespan 里 `scheduler.start()` 注释说「每分钟推送 bar」，但从没注册 job；`run_strategy` 只是把策略挂上 manager，没有数据源推动。本任务补上 30 秒间隔 feed。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_feed.py
import pytest
import sys
sys.path.insert(0, "services/strategy-engine")

from app.engine.manager import StrategyManager
from strategies.base import BaseStrategy, BarData, SignalAction


class NoopStrategy(BaseStrategy):
    name = "noop"
    version = "1.0.0"
    description = "record received bars"
    def initialize(self, params: dict):
        self._initialized = True
        self.received_codes = []
    def on_bar(self, bar: BarData):
        self.received_codes.append(bar.code)
        return None


@pytest.mark.asyncio
async def test_feed_bars_feeds_running_strategy(monkeypatch):
    from app import feed

    class FakeClient:
        async def get_quote(self, code, provider="mock"):
            return {"code": code, "price": 12.34, "volume": 8888}
        async def get_stock_list(self, **kwargs):
            return [{"code": "600000", "name": "浦发银行"}]

    manager = StrategyManager()
    await manager.initialize()
    strat = NoopStrategy("S1", {"code": "600000"})
    strat.initialize({"code": "600000"})
    manager.add_strategy(strat)
    manager._strategies["S1"]._status = StrategyStatus.RUNNING  # 替换 RUNNING 枚举
    monkeypatch.setattr(feed, "strategy_manager", manager)
    monkeypatch.setattr(feed, "data_client", FakeClient())

    await feed.feed_market_bars()

    assert strat.received_codes == ["600000"]
```

注意 `StrategyStatus` 需从 `strategies.base` 导入补在顶部 `from strategies.base import StrategyStatus`。

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_feed.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.feed'`

- [ ] **Step 3: 实现 feed.py**

创建 `services/strategy-engine/app/feed.py`：

```python
"""
行情喂料任务：从 data-service 拉取实时 quote，转成 BarData 推给运行中的策略
"""
import logging
from typing import Optional, Dict, Any

from app.data_client import data_client
from app.engine.manager import strategy_manager
from strategies.base import BarData, StrategyStatus

logger = logging.getLogger(__name__)


async def fetch_bar(code: str) -> Optional[dict]:
    """拉取单只股票 quote 并转为 BarData dict（失败返回 None）"""
    quote = await data_client.get_quote(code, provider="mock")
    if not quote:
        return None
    price = float(quote.get("price", 0))
    if price <= 0:
        return None
    return BarData(
        code=code,
        timestamp=quote.get("timestamp"),
        open=price,
        high=price,
        low=price,
        close=price,
        volume=int(quote.get("volume", 0) or 0),
    ).to_dict()


async def feed_market_bars():
    """对所有运行中策略的标的拉取行情并喂给策略管理器"""
    running = strategy_manager.get_running_strategies()
    if not running:
        return
    codes = {s.params.get("code") or "600000" for s in running}
    for code in codes:
        bar = await fetch_bar(code)
        if bar:
            await strategy_manager.process_bars(bar)
```

在 `services/strategy-engine/app/main.py` lifespan 中注册 job（第 33-35 行之间）：

```python
    scheduler.start()
    from app.feed import feed_market_bars
    # 每 30 秒把最新 quote 喂给运行中的策略
    scheduler.add_interval_job("feed_bars", feed_market_bars, seconds=30)
```

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_feed.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: 提交**

```bash
git add services/strategy-engine/app/feed.py services/strategy-engine/app/main.py tests/test_feed.py
git commit -m "feat(strategy-engine): schedule feed_bars job pushing quotes to strategies"
```

---

### Task 8: 信号落库 + 桥接到交易（strategy → trading）

**Files:**

- Create: `services/strategy-engine/app/bridge.py`
- Modify: `services/strategy-engine/app/main.py:29-37`
- Modify: `services/trading-service/app/routers/trading.py:39`
- Modify: `services/trading-service/app/engine.py:272-291`
- Test: `tests/test_bridge.py`

**Interfaces:**

- Consumes: `StrategySignal`(strategies.base)、`strategy_manager`、`common.SessionLocal`、`common.models.strategy.StrategySignal`
- Produces: `register_handlers()` —— 注册 `_on_signal(signal)`；信号 → (a) 落 `strategy_signals` 表 (b) `settings.trading.TRADING_ENABLED` 时 POST `http://trading-service:8003/trading/order` 并回喂 `strategy.on_trade`

**背景：** 交易服务 `POST /trading/order` 现在强制 `get_current_user`，内部桥接调用会被 401；`submit_order` 调 risk 时漏传必填 `order_no`（`RiskCheckRequest` 要求），导致 422 走降级分支，风控形同虚设。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_bridge.py
import asyncio
import pytest
import sys
sys.path.insert(0, "services/strategy-engine")

from strategies.base import StrategySignal, SignalAction


@pytest.mark.asyncio
async def test_bridge_persists_signal_and_posts_order(monkeypatch):
    from app import bridge

    seen = {}
    posted = {"calls": []}

    class FakeSession:
        def __init__(self, **kw):
            self.rows = []
            self._closed = False
        def add(self, row):
            self.rows.append(row)
        def commit(self):
            seen["rows"] = self.rows
        def close(self):
            self._closed = True

    monkeypatch.setattr(bridge, "SessionLocal", FakeSession)

    class FakeClient:
        def __init__(self, *a, **k):
            pass
        async def __aenter__(self):
            return self
        async def __aexit__(self, *a):
            return False
        async def post(self, url, json=None, headers=None):
            posted["calls"].append((url, json))
            class R:
                status_code = 200
            return R()

    monkeypatch.setattr("httpx.AsyncClient", FakeClient)

    sig = StrategySignal(
        strategy_id="S000001", code="600000", action=SignalAction.BUY,
        price=10.0, quantity=100, reason="test",
    )
    await bridge.handle_signal(sig)

    assert len(seen["rows"]) == 1   # strategy_signals 落一行
    assert posted["calls"], "应 POST 到 trading-service"
    url, payload = posted["calls"][0]
    assert "/trading/order" in url
    assert payload["code"] == "600000"
    assert payload["quantity"] == 100
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_bridge.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.bridge'`

- [ ] **Step 3: 实现 bridge.py**

创建 `services/strategy-engine/app/bridge.py`：

```python
"""
信号桥接：策略信号 → 落库 strategy_signals + 桥接 trading-service 下单
"""
import asyncio
import logging
from typing import Callable

import httpx

from common import get_logger, SessionLocal
from common.models.strategy import StrategySignal as StrategySignalRow
from strategies.base import StrategySignal, SignalAction

logger = get_logger("strategy-engine")

TRADING_SERVICE_URL = "http://trading-service:8003"


async def persist_signal(signal: StrategySignal, db_factory=SessionLocal):
    """把策略信号写入 strategy_signals 表"""
    # strategy_id 形如 "S000001"，取数字部分对应 strategies.id
    numeric_id = 0
    if signal.strategy_id.startswith("S"):
        try:
            numeric_id = int(signal.strategy_id[1:])
        except ValueError:
            numeric_id = 0
    row = StrategySignalRow(
        strategy_id=numeric_id,
        code=signal.code,
        action=signal.action.value,
        price=float(signal.price),
        quantity=int(signal.quantity),
        reason=signal.reason,
        strength=float(signal.strength),
    )
    db = db_factory()
    try:
        db.add(row)
        db.commit()
        logger.info("Strategy signal persisted", code=signal.code, action=signal.action.value)
    finally:
        db.close()


async def sync_strategy_position(signal: StrategySignal):
    """把已成交信号回喂给策略实例，保持策略内部持仓一致"""
    from app.engine.manager import strategy_manager
    strategy = strategy_manager.get_strategy(signal.strategy_id)
    if strategy is None:
        return
    try:
        await asyncio.get_event_loop().run_in_executor(
            None,
            lambda: strategy.on_trade({
                "code": signal.code,
                "direction": signal.action.value,
                "quantity": int(signal.quantity),
            }),
        )
    except Exception as e:
        logger.warning(f"Failed to sync strategy position: {e}")


async def submit_order(signal: StrategySignal):
    """POST trading-service 下单（market 单，价格用信号价格）"""
    payload = {
        "strategy_id": int(signal.strategy_id[1:]) if signal.strategy_id.startswith("S") else None,
        "code": signal.code,
        "direction": signal.action.value,
        "order_type": "market",
        "price": float(signal.price),
        "quantity": int(signal.quantity),
    }
    async with httpx.AsyncClient(timeout=10.0) as client:
        resp = await client.post(
            f"{TRADING_SERVICE_URL}/trading/order",
            json=payload,
            headers={"Content-Type": "application/json"},
        )
        if resp.status_code != 201:
            logger.error(f"Order submission failed: {resp.status_code} {resp.text}")
            return
        logger.info(f"Order submitted: {payload['code']} {payload['direction']} x{payload['quantity']}")
        await sync_strategy_position(signal)


async def handle_signal(signal: StrategySignal):
    """信号处理入口：先落库，再按开关决定是否下单"""
    try:
        await persist_signal(signal)
    except Exception as e:
        logger.error(f"persist_signal failed: {e}")

    from common import settings
    if signal.action in (SignalAction.BUY, SignalAction.SELL) and settings.trading.TRADING_ENABLED:
        await submit_order(signal)


def register_handlers():
    """注册同步回调包装（manager 的回调是同步调用，这里转成异步任务）"""
    from app.engine.manager import strategy_manager
    from common import settings

    def _on_signal(signal: StrategySignal):
        try:
            asyncio.get_event_loop().create_task(handle_signal(signal))
        except RuntimeError:
            asyncio.run(handle_signal(signal))  # 兜底：无事件循环时

    strategy_manager.register_signal_callback(_on_signal)
    logger.info("Signal bridge handlers registered (TRADING_ENABLED=%s)",
                settings.trading.TRADING_ENABLED)
```

在 `services/strategy-engine/app/main.py` lifespan（feed job 注册之后）追加：

```python
    from app.bridge import register_handlers
    register_handlers()
```

- [ ] **Step 4: 修改 trading 认证与风控 payload**

`services/trading-service/app/routers/trading.py`：第 21-22 行导入加 `get_optional_user`，第 39 行：

```python
    current_user: Optional[AuthUser] = Depends(get_optional_user),
```

（`Optional` 已在文件顶部第 7 行导入。）

`services/trading-service/app/engine.py`：`submit_order` 中 risk 请求体补 `order_no`（第 274-283 行）：

```python
                    json={
                        "order_no": order.order_id,
                        "code": order.code,
                        "direction": order.direction.value,
                        "price": order.price,
                        "quantity": order.quantity,
                    },
```

- [ ] **Step 5: 运行确认通过**

Run: `py -m pytest tests/test_bridge.py -v`
Expected: PASS (1 passed)

Run: `py -m py_compile services/strategy-engine/app/bridge.py services/trading-service/app/routers/trading.py services/trading-service/app/engine.py`
Expected: 无输出

- [ ] **Step 6: 提交**

```bash
git add services/strategy-engine/app/bridge.py services/strategy-engine/app/main.py services/trading-service/app/routers/trading.py services/trading-service/app/engine.py tests/test_bridge.py
git commit -m "feat(loop): bridge strategy signals to trading-service, relax internal auth"
```

---

## R3 交易/持仓/风控持久化

### Task 9: trading-service 订单与成交持久化

**Files:**

- Create: `services/trading-service/app/persistence.py`
- Modify: `services/trading-service/app/routers/trading.py:35-92,177-202`
- Modify: `services/trading-service/app/main.py:27-33`
- Test: `tests/test_persistence_trading.py`

**Interfaces:**

- Consumes: `common.models.order.Order/Trade/OrderStatus/OrderDirection/OrderType`
- Produces: `save_order(order, db_factory=SessionLocal)`、`save_trade(trade, db_factory=SessionLocal)`、`update_order_status(order, db_factory=SessionLocal)`、`rebuild_engine(engine, db_factory=SessionLocal)`

**背景：** `orders/trades` 表已存在（common Base），但没有任何写入。设计：路由层双写（引擎内存为快路径），启动时从 DB 重建引擎（订单、成交、资金、持仓）。

- [ ] **Step 1: 写失败测试（用 SQLite 验证双写与重建）**

```python
# tests/test_persistence_trading.py
import pytest
import sys
sys.path.insert(0, "services/trading-service")
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

from common import Base, models  # noqa: F401 注册表
from app.engine import TradingEngine, OrderDirection, OrderType


@pytest.fixture
def db_factory():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(bind=engine)
    return sessionmaker(autocommit=False, autoflush=False, bind=engine)


@pytest.mark.asyncio
async def test_order_flow_persisted_and_rebuilt(db_factory):
    from app.persistence import save_order, save_trade, rebuild_engine

    engine = TradingEngine()
    await engine.initialize(initial_capital=100000.0)
    engine._positions = {}  # 不依赖内部网络调用，跳过 submit_order（避免 HTTP 到 risk/portfolio）
    order = engine.create_order("600000", OrderDirection.BUY, OrderType.MARKET, 10.0, 100)
    # 手工模拟成交状态变更（不调用 submit_order，以免 httpx 请求外部服务）
    order.status = engine.OrderStatus.FILLED if hasattr(engine, "OrderStatus") else None

    def fake_prune():
        # create_order 里 emit 事件会引用 self._callbacks，安全；仅填充状态
        order.filled_quantity = 100
        order.avg_price = 10.01
        order.status = OrderStatus.FILLED  # from common.models.order

    from common.models.order import OrderStatus, Trade
    order.filled_quantity = 100
    order.avg_price = 10.01
    order.status = OrderStatus.FILLED  # 用字符串兼容：SQLAlchemy enum 存值
    # 直接用 SQLAlchemy enum 值写行
    save_order(order, db_factory=db_factory)

    db = db_factory()
    assert db.query(models.Order).count() == 1
    db.close()

    # 重建
    e2 = TradingEngine()
    traded = []
    rebuild_engine(e2, db_factory=db_factory)
    assert e2.get_order(order.order_id) is not None
```

> 注意 `common.models.order.OrderStatus` 是 enum，存储为字符串值；`persistence.py` 读写时统一用 `.value` 转换。

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_persistence_trading.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.persistence'`

- [ ] **Step 3: 实现 persistence.py**

创建 `services/trading-service/app/persistence.py`：

```python
"""
交易持久化：订单/成交 双写 DB，以及从 DB 重建引擎
"""
import logging
from typing import Optional

from common import SessionLocal, get_logger
from common.models.order import (
    Order as OrderRow,
    Trade as TradeRow,
    OrderStatus as OrderStatusRow,
    OrderDirection as OrderDirectionRow,
    OrderType as OrderTypeRow,
)

logger = get_logger("trading-service")


def _order_status(o) -> str:
    return o.status.value if hasattr(o.status, "value") else str(o.status)


def save_order(order, db_factory=SessionLocal):
    """把运行时订单对象写/更新到 orders 表（order_no 作唯一键）"""
    db = db_factory()
    try:
        row = db.query(OrderRow).filter_by(order_no=order.order_id).first()
        if row is None:
            row = OrderRow(order_no=order.order_id)
            db.add(row)
        row.strategy_id = order.strategy_id
        row.code = order.code
        row.direction = order.direction.value if hasattr(order.direction, "value") else order.direction
        row.order_type = order.order_type.value if hasattr(order.order_type, "value") else order.order_type
        row.price = order.price
        row.quantity = int(order.quantity)
        row.filled_quantity = int(order.filled_quantity)
        row.avg_price = order.avg_price
        row.status = order.status
        row.submit_time = getattr(order, "submit_time", None) or getattr(order, "create_time", None) or datetime.now()
        row.filled_time = getattr(order, "filled_time", None)
        row.update_time = getattr(order, "update_time", None) or datetime.now()
        row.reject_reason = getattr(order, "reject_reason", None)
        row.extra = getattr(order, "metadata", {})
        db.commit()
        logger.info("Order persisted", order_no=order.order_id, status=row.status.value if hasattr(row.status, "value") else row.status)
    finally:
        db.close()


def save_trade(trade, db_factory=SessionLocal):
    """把成交记录写入 trades 表"""
    db = db_factory()
    try:
        row = TradeRow(
            order_no=trade.order_id,
            code=trade.code,
            direction=trade.direction.value if hasattr(trade.direction, "value") else trade.direction,
            price=trade.price,
            quantity=int(trade.quantity),
            commission=float(trade.commission),
            slip=float(trade.slippage),
            trade_time=datetime.fromisoformat(trade.trade_time.replace("Z", "+00:00"))
            if isinstance(trade.trade_time, str) else trade.trade_time,
            extra={"tax": 0.0},
        )
        db.add(row)
        db.commit()
        logger.info("Trade persisted", order_no=trade.order_id, code=trade.code)
    finally:
        db.close()


def rebuild_engine(engine, db_factory=SessionLocal, initial_capital: float = 100000.0):
    """从 DB 重建引擎：订单、成交、资金、持仓（按成交重放）"""
    from datetime import datetime
    from app.engine import Order, Trade, OrderDirection, OrderType, OrderStatus

    db = db_factory()
    try:
        order_rows = db.query(OrderRow).order_by(OrderRow.id).all()
        trade_rows = db.query(TradeRow).order_by(TradeRow.id).all()
    finally:
        db.close()

    # 重建订单字典
    for r in order_rows:
        direction = OrderDirection(r.direction.value) if hasattr(r.direction, "value") else OrderDirection(str(r.direction))
        otype = OrderType(r.order_type.value) if hasattr(r.order_type, "value") else OrderType(str(r.order_type))
        status = OrderStatus(r.status.value) if hasattr(r.status, "value") else OrderStatus(str(r.status))
        engine._orders[r.order_no] = Order(
            order_id=r.order_no,
            strategy_id=r.strategy_id,
            code=r.code,
            direction=direction,
            order_type=otype,
            price=r.price,
            quantity=r.quantity,
            filled_quantity=r.filled_quantity or 0,
            avg_price=r.avg_price or 0.0,
            status=status,
            create_time=r.submit_time or datetime.now(),
            update_time=r.update_time or datetime.now(),
            submit_time=r.submit_time,
            filled_time=r.filled_time,
            reject_reason=r.reject_reason,
            metadata=r.extra or {},
        )

    # 重放成交恢复资金与持仓
    capital = float(initial_capital)
    positions = {}
    for t in trade_rows:
        amount = t.price * t.quantity
        tax = float((t.extra or {}).get("tax", 0.0))
        if OrderDirection(t.direction.value) == OrderDirection.BUY:
            capital -= amount + t.commission
            pos = positions.setdefault(t.code, {"quantity": 0, "avg_price": 0.0, "frozen": 0})
            total = pos["quantity"] + t.quantity
            pos["avg_price"] = (pos["avg_price"] * pos["quantity"] + t.price * t.quantity) / total
            pos["quantity"] = total
        else:
            capital += amount - t.commission - tax
            pos = positions.get(t.code)
            if pos:
                pos["quantity"] -= t.quantity
                if pos["quantity"] <= 0:
                    del positions[t.code]
        engine._trades.append(Trade(
            trade_id=f"T{len(engine._trades)+1:06d}",
            order_id=t.order_no,
            code=t.code,
            direction=OrderDirection(t.direction.value),
            price=t.price,
            quantity=t.quantity,
            amount=amount,
            commission=t.commission,
            slippage=t.slip or 0.0,
            trade_time=t.trade_time or datetime.now(),
            metadata=t.extra or {},
        ))
    engine._capital = capital
    engine._positions = positions
    logger.info("Trading engine rebuilt from DB", orders=len(engine._orders), trades=len(trade_rows))
```

`services/trading-service/app/routers/trading.py`：

- `create_order`（第 52-85 行）：`trading_engine.submit_order(...)` 成功后追加双写：

```python
        # 提交订单
        success = await trading_engine.submit_order(order.order_id)

        if not success:
            raise_trading_error("订单提交失败", order.reject_reason)

        # 双写持久化
        from app.persistence import save_order, save_trade
        save_order(order, db_factory=SessionLocal if "SessionLocal" in dir() else None)  # 保持默认 SessionLocal
        for t in trading_engine.get_trades(limit=1000):
            if t.order_id == order.order_id:
                save_trade(t)
```

> 简化写法：持久化默认用 `common.SessionLocal`，测试传 `db_factory` 即可。这里直接用默认（即 docker 中 MySQL）。

- `cancel_order`（第 191-202 行）：取消成功后追加：

```python
    from app.persistence import save_order
    save_order(trading_engine.get_order(order_id))
```

`services/trading-service/app/main.py`（Task 4 已加 init_db）在 lifespan 里初始化引擎后重建：

```python
    from app.engine import trading_engine
    await trading_engine.initialize(initial_capital=100000.0)
    from app.persistence import rebuild_engine
    rebuild_engine(trading_engine)
```

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_persistence_trading.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add services/trading-service/app/persistence.py services/trading-service/app/routers/trading.py services/trading-service/app/main.py tests/test_persistence_trading.py
git commit -m "feat(trading): persist orders/trades, rebuild engine from DB on startup"
```

---

### Task 10: portfolio-service 持仓/资金/流水持久化

**Files:**

- Create: `services/portfolio-service/app/persistence.py`
- Modify: `services/portfolio-service/app/routers/portfolio.py:23-130`
- Modify: `services/portfolio-service/app/main.py:27-36`
- Test: `tests/test_persistence_portfolio.py`

**Interfaces:**

- Consumes: `common.models.portfolio.Position/Account/AccountLog/PositionDirection/PositionStatus`；`portfolio_engine`
- Produces: `save_state(engine, db_factory=SessionLocal)`（账户 UPSERT + 持仓 UPSERT + 流水追加）；`load_state(engine, db_factory=SessionLocal)`（启动恢复）

**背景：** portfolio 引擎只存内存，进程重启全丢。改造为：所有变更路由（process-trade/deposit/withdraw/update-prices）调 `save_state`，启动时 `load_state`。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_persistence_portfolio.py
import pytest
import sys
sys.path.insert(0, "services/portfolio-service")
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

from common import Base, models  # noqa: F401
from app.engine import PortfolioEngine


@pytest.fixture
def db_factory():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(bind=engine)
    return sessionmaker(autocommit=False, autoflush=False, bind=engine)


@pytest.mark.asyncio
async def test_portfolio_state_persisted_and_loaded(db_factory):
    from app.persistence import save_state, load_state

    engine = PortfolioEngine()
    await engine.initialize(initial_capital=100000.0)
    engine.process_trade("600000", "buy", 10.0, 1000, commission=5.0)
    engine.process_trade("600000", "sell", 12.0, 400, commission=5.0)
    save_state(engine, db_factory=db_factory)

    db = db_factory()
    assert db.query(models.Account).count() == 1
    assert db.query(models.Position).count() == 1
    db.close()

    e2 = PortfolioEngine()
    await e2.initialize(initial_capital=100000.0)
    load_state(e2, db_factory=db_factory)
    assert e2.get_position("600000") is not None
    assert e2.get_position("600000").quantity == 600
    summary = e2.get_summary()
    assert summary["cash"] < 99990.0  # 卖出后资金已包含部分回笼
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_persistence_portfolio.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.persistence'`

- [ ] **Step 3: 实现 persistence.py**

创建 `services/portfolio-service/app/persistence.py`：

```python
"""
组合持久化：账户/持仓/流水 双写 DB，以及启动时恢复
"""
import logging
from datetime import datetime

from common import SessionLocal, get_logger
from common.models.portfolio import (
    Position as PositionRow,
    Account as AccountRow,
    AccountLog as AccountLogRow,
    PositionDirection as PositionDirectionRow,
    PositionStatus as PositionStatusRow,
)

logger = get_logger("portfolio-service")

DEFAULT_ACCOUNT_NO = "DEFAULT-001"


def save_state(engine, db_factory=SessionLocal):
    """把引擎当前账户/持仓/流水写库（账户与持仓 UPSERT，新增流水去重追加）"""
    db = db_factory()
    try:
        account = engine.get_account()
        acc = db.query(AccountRow).filter_by(account_no=account.account_no).first()
        if acc is None:
            acc = AccountRow(account_no=account.account_no)
            db.add(acc)
        acc.account_type = account.account_type
        acc.balance = account.balance
        acc.frozen_balance = account.frozen_balance
        acc.total_deposited = account.total_deposited
        acc.total_withdrawn = account.total_withdrawn
        acc.total_profit = account.total_profit
        acc.status = account.status
        db.flush()

        for pos in engine.get_positions():
            prow = db.query(PositionRow).filter_by(
                code=pos.code, status=pos.status.value
            ).first()
            if prow is None:
                prow = PositionRow(code=pos.code, status=pos.status.value)
                db.add(prow)
            prow.name = pos.name or ""
            prow.direction = pos.direction.value
            prow.quantity = int(pos.quantity)
            prow.avg_cost = pos.avg_cost
            prow.avg_price = pos.avg_price
            prow.current_price = pos.current_price
            prow.frozen_quantity = int(pos.frozen_quantity)
            prow.updated_at = pos.updated_at or datetime.now()
            prow.extra = {
                "realized_pnl": pos.realized_pnl,
                "total_buy_amount": pos.total_buy_amount,
                "total_sell_amount": pos.total_sell_amount,
                "total_commission": pos.total_commission,
                "total_tax": pos.total_tax,
            }

        # 追加新流水（按 related_order_no + type + amount 判重，避免重复写）
        existing = {
            (r.account_no, r.related_order_no, r.type, r.amount)
            for r in db.query(AccountLogRow).filter_by(account_no=account.account_no).all()
        }
        for log in engine.get_logs(limit=100000):
            key = (log.account_no, log.related_order_no, log.type, log.amount)
            if key in existing:
                continue
            db.add(AccountLogRow(
                account_no=log.account_no,
                type=log.type,
                amount=log.amount,
                balance_after=log.balance_after,
                description=log.description,
                related_order_no=log.related_order_no,
                created_at=log.created_at or datetime.now(),
            ))
        db.commit()
        logger.info("Portfolio state saved", account=account.account_no)
    finally:
        db.close()


def load_state(engine, db_factory=SessionLocal):
    """启动时从 DB 恢复账户与持仓"""
    from app.engine import Position, Account, PositionDirection, PositionStatus

    db = db_factory()
    try:
        acc = db.query(AccountRow).filter_by(account_no=DEFAULT_ACCOUNT_NO).first()
        pos_rows = db.query(PositionRow).all()
    finally:
        db.close()

    if acc is not None:
        engine._account = Account(
            account_no=acc.account_no,
            account_type=acc.account_type or "cash",
            balance=acc.balance or 0.0,
            frozen_balance=acc.frozen_balance or 0.0,
            total_deposited=acc.total_deposited or 0.0,
            total_withdrawn=acc.total_withdrawn or 0.0,
            total_profit=acc.total_profit or 0.0,
            status=acc.status or "active",
        )
        engine._initial_capital = engine._account.balance + (engine._account.total_withdrawn or 0)
    else:
        engine._account = Account(
            account_no=DEFAULT_ACCOUNT_NO,
            balance=engine._initial_capital,
            total_deposited=engine._initial_capital,
        )

    for p in pos_rows:
        pos = Position(
            code=p.code,
            name=p.name or "",
            direction=PositionDirection(p.direction.value),
            quantity=int(p.quantity or 0),
            avg_cost=p.avg_cost or 0.0,
            avg_price=p.avg_price or 0.0,
            current_price=p.current_price or 0.0,
            frozen_quantity=int(p.frozen_quantity or 0),
            status=PositionStatus(p.status.value),
            created_at=p.created_at or datetime.now(),
            updated_at=p.updated_at or datetime.now(),
            closed_at=p.closed_at,
        )
        extra = p.extra or {}
        pos.realized_pnl = float(extra.get("realized_pnl", 0.0))
        pos.total_buy_amount = float(extra.get("total_buy_amount", 0.0))
        pos.total_sell_amount = float(extra.get("total_sell_amount", 0.0))
        pos.total_commission = float(extra.get("total_commission", 0.0))
        pos.total_tax = float(extra.get("total_tax", 0.0))
        engine._positions[p.code] = pos
    engine._initialized = True
    engine._running = True
    logger.info("Portfolio engine restored from DB", positions=len(engine._positions))
```

`services/portfolio-service/app/routers/portfolio.py`：

- `process_trade`（第 129-150 行）在 `engine.process_trade` 成功后追加：

```python
        # 双写持久化
        from app.persistence import save_state
        save_state(portfolio_engine)
```

- `deposit`（第 91-100 行）、`withdraw`（第 103-112 行）、`update_prices`（第 115-119 行）后各追加一行 `save_state(portfolio_engine)`。

`services/portfolio-service/app/main.py`（Task 4 已加 init_db）：

```python
    from app.engine import portfolio_engine
    await portfolio_engine.initialize(initial_capital=100000.0)
    from app.persistence import load_state
    load_state(portfolio_engine)
```

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_persistence_portfolio.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add services/portfolio-service/app/persistence.py services/portfolio-service/app/routers/portfolio.py services/portfolio-service/app/main.py tests/test_persistence_portfolio.py
git commit -m "feat(portfolio): persist account/positions/logs, restore on startup"
```

---

### Task 11: risk-service 限额与检查历史持久化

**Files:**

- Create: `services/risk-service/app/persistence.py`
- Modify: `services/risk-service/app/routers/risk.py:35-92,128-233`
- Modify: `services/risk-service/app/main.py:27-37`
- Test: `tests/test_persistence_risk.py`

**Interfaces:**

- Consumes: `common.models.risk.RiskLimit/RiskCheck/RiskType/RiskLevel`
- Produces: `save_limits(engine, db_factory=SessionLocal)`、`save_check(engine, request, results, db_factory=SessionLocal)`、`load_limits(engine, db_factory=SessionLocal)`

**背景：** `risk_limits/risk_checks` 表已存在但无写入。启动时若表里有限额则用 DB（否则用默认限额）；`/check` 每次把两次子检查各写一行 `risk_checks`。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_persistence_risk.py
import pytest
import sys
sys.path.insert(0, "services/risk-service")
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

from common import Base, models  # noqa: F401
from app.engine import RiskEngine, RiskLimit, RiskType


@pytest.fixture
def db_factory():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(bind=engine)
    return sessionmaker(autocommit=False, autoflush=False, bind=engine)


@pytest.mark.asyncio
async def test_limits_and_checks_persisted(db_factory):
    from app.persistence import save_limits, save_check, load_limits

    engine = RiskEngine()
    await engine.initialize()

    limit = RiskLimit(limit_id="l1", limit_name="Test", risk_type=RiskType.POSITION_LIMIT,
                      limit_value=0.3, warning_value=0.24)
    engine.add_limit(limit)
    save_limits(engine, db_factory=db_factory)

    db = db_factory()
    assert db.query(models.RiskLimit).count() >= 1
    db.close()

    e2 = RiskEngine()
    await e2.initialize()
    load_limits(e2, db_factory=db_factory)
    assert e2.get_limit("l1") is not None
```

- [ ] **Step 2: 运行确认失败**

Run: `py -m pytest tests/test_persistence_risk.py -v`
Expected: FAIL —— `ModuleNotFoundError: No module named 'app.persistence'`

- [ ] **Step 3: 实现 persistence.py**

创建 `services/risk-service/app/persistence.py`：

```python
"""
风控持久化：限额与检查历史写库，启动时恢复限额
"""
import logging
from datetime import datetime

from common import SessionLocal, get_logger
from common.models.risk import (
    RiskLimit as RiskLimitRow,
    RiskCheck as RiskCheckRow,
    RiskType as RiskTypeRow,
    RiskLevel as RiskLevelRow,
)

logger = get_logger("risk-service")


def save_limits(engine, db_factory=SessionLocal):
    """把引擎限额全量 UPSERT 到 risk_limits 表"""
    db = db_factory()
    try:
        for limit in engine.get_limits():
            row = db.query(RiskLimitRow).filter_by(limit_name=limit.limit_name).first()
            if row is None:
                row = RiskLimitRow(limit_name=limit.limit_name)
                db.add(row)
            row.risk_type = limit.risk_type.value
            row.limit_value = float(limit.limit_value)
            row.warning_value = limit.warning_value
            row.description = limit.description
            row.enabled = bool(limit.enabled)
            row.updated_at = datetime.now()
        db.commit()
        logger.info("Risk limits saved", count=len(engine.get_limits()))
    finally:
        db.close()


def save_check(order_no, code, direction, checks, db_factory=SessionLocal):
    """把一次 /check 的两次子检查写库（position_limit + single_trade_limit）"""
    db = db_factory()
    try:
        for c in checks:
            db.add(RiskCheckRow(
                order_no=order_no,
                code=code,
                direction=direction,
                price=None,
                quantity=None,
                risk_type=c.risk_type.value,
                limit_value=c.limit_value,
                actual_value=c.actual_value,
                passed=bool(c.passed),
                level=c.level.value,
                message=c.message,
                created_at=datetime.now(),
            ))
        db.commit()
        logger.info("Risk checks saved", order_no=order_no, count=len(checks))
    finally:
        db.close()


def load_limits(engine, db_factory=SessionLocal):
    """启动时若 DB 有限额则覆盖引擎默认限额（否则保留默认）"""
    from app.engine import RiskLimit, RiskType

    db = db_factory()
    try:
        rows = db.query(RiskLimitRow).all()
    finally:
        db.close()

    if not rows:
        return
    for r in rows:
        engine.add_limit(RiskLimit(
            limit_id=f"db_{r.id}",
            limit_name=r.limit_name,
            risk_type=RiskType(r.risk_type.value),
            limit_value=float(r.limit_value),
            warning_value=r.warning_value,
            description=r.description,
            enabled=bool(r.enabled),
            update_time=r.updated_at or datetime.now(),
        ))
    logger.info("Risk limits restored", count=len(rows))
```

`services/risk-service/app/routers/risk.py`：

- 导入区加 `from app.persistence import save_limits, save_check, load_limits`（函数内 import 亦可）。
- `check_risk`（第 53-88 行）在 `return {...}` 前追加：

```python
        # 持久化检查历史
        save_check(request.order_no, request.code, request.direction.value,
                   [position_result, trade_result])
```

- `create_limit`（第 154 行 `risk_engine.add_limit(limit)` 后）与 `update_limit`（第 191 行后）、`delete_limit`（第 221 行后）各追加 `save_limits(risk_engine)`。

`services/risk-service/app/main.py`（Task 4 已加 init_db）在 `await risk_engine.initialize()` 后追加：

```python
    from app.persistence import load_limits
    load_limits(risk_engine)
```

（risk-service 目前 lifespan 未 initialize risk_engine；改为先 initialize 再 load_limits。）

- [ ] **Step 4: 运行确认通过**

Run: `py -m pytest tests/test_persistence_risk.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add services/risk-service/app/persistence.py services/risk-service/app/routers/risk.py services/risk-service/app/main.py tests/test_persistence_risk.py
git commit -m "feat(risk): persist limits and check history, restore limits on startup"
```

---

## R4 关键链路测试

### Task 12: 重写 tests/conftest.py（真实 App + SQLite + 认证 + HTTP 桩）

**Files:**

- Rewrite: `tests/conftest.py`
- Fix: `tests/test_strategy_api.py`

**Interfaces:**

- Consumes: 各服务 `app.main.app`；`common.auth_service`；`common.get_db`
- Produces: 测试级 `client` 夹具（带登录 token 的 TestClient）；`db_factory`（SQLite StaticPool 内存库）；`fake_httpx`（拦截 + 可配置远端响应）

**背景：** 当前 conftest 注释掉了路由注册与 `get_db` override，`test_strategy_api.test_list_strategies_empty` 404。需要：①把 `services` 与各服务目录加入 sys.path；②用真实 App；③override `common.get_db` 为 SQLite 会话；④生成 admin token；⑤后续测试里可 mock 外部 HTTP。

- [ ] **Step 1: 重写 conftest.py**

```python
"""
pytest 配置：真实 App + SQLite 内存库 + admin token + HTTP 桩
"""
import sys
import pathlib
from typing import Generator

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

ROOT = pathlib.Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "services"))
for svc in ("data-service", "strategy-engine", "backtest-service",
            "trading-service", "portfolio-service", "risk-service"):
    sys.path.insert(0, str(ROOT / "services" / svc))

import common
import common.models  # noqa: F401 注册所有表
from common import Base, get_db, auth_service


# 内存 SQLite（StaticPool 保证单连接复用）
_TEST_ENGINE = create_engine(
    "sqlite://",
    connect_args={"check_same_thread": False},
    poolclass=StaticPool,
)
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=_TEST_ENGINE)


@pytest.fixture(scope="session", autouse=True)
def _prepare_db_and_auth():
    """整个测试会话：建表 + 幂等初始化 auth_service（admin/admin123）"""
    Base.metadata.create_all(bind=_TEST_ENGINE)
    import asyncio

    async def _init():
        await auth_service.initialize("dev-secret-key-quant-system-2026")

    asyncio.run(_init())


@pytest.fixture
def db_factory():
    """SQLite 会话工厂，供 app.persistence 等以 db_factory= 参数使用"""
    return TestingSessionLocal


@pytest.fixture
def auth_token() -> str:
    """返回 admin 的 access token"""
    async def _auth():
        result = await auth_service.authenticate("admin", "admin123")
        assert result, "admin 认证失败"
        return result["access_token"]["token"]
    import asyncio
    return asyncio.run(_auth())


@pytest.fixture
def client_factory(auth_token):
    """按服务名构建已 override get_db 的 TestClient 工厂"""
    def build(app_module):
        app = app_module.app
        app.dependency_overrides[get_db] = lambda: TestingSessionLocal()
        client = TestClient(app)
        client.headers["Authorization"] = f"Bearer {auth_token}"
        return client
    return build


@pytest.fixture
def client(client_factory) -> Generator:
    """默认 strategy-engine 客户端的别名（供既有测试使用）"""
    import app.main as seam
    with client_factory(seam) as c:
        yield c
```

- [ ] **Step 2: 修复 test_strategy_api.py**

- 把顶部 import 改为 `from fastapi.testclient import TestClient`（保留）。
- 把 `client` fixture 用法保留（conftest 已提供）。
- `test_run_strategy`/`test_stop_strategy` 依赖 DB 策略记录；策略创建需要 token（create 要求 `get_current_user`）⇒ conftest 的 `client` 已带 header，现有测试无需改 body，但 `sample_strategy_data` 的 `type` 用 `"technical"` 会 fallback 到 `DualMAStrategy`，可运行——保持现状即可跑通。
- 若 `test_create_strategy_duplicate_code` 期望 400：`raise_validation_error("策略代码 ... 已存在")` 会经异常处理器返回 state = 400？需要确认 `ValidationException` 映射 400。若映射为 422，把断言改为 422。计划标注：**实现时运行后按实际状态码修正断言**（400 或 422 二选一，以 `ValidationException` 的 HTTP 状态为准）。

```python
# 修改后 test_create_strategy_duplicate_code 的断言
    response = client.post("/strategy/", json=sample_strategy_data)
    # raise_validation_error 状态码依 common.exceptions 而定；若为 422 则改断言
    assert response.status_code in (400, 422)
    assert "已存在" in response.json()["message"]
```

- [ ] **Step 3: 运行既有策略测试确认通过**

Run: `py -m pytest tests/test_strategy_api.py -v`
Expected: PASS（11 passed；duplicate 断言按上面允许 400/422）

- [ ] **Step 4: 提交**

```bash
git add tests/conftest.py tests/test_strategy_api.py
git commit -m "test: real-app clients with sqlite+auth, fix strategy api tests"
```

---

### Task 13: 新增关键链路测试文件

**Files:**

- Create: `tests/test_auth.py`
- Create: `tests/test_data_service.py`
- Create: `tests/test_trading.py`
- Create: `tests/test_risk_api.py`
- Create: `tests/test_portfolio_api.py`
- Create: `tests/test_backtest_api.py`
- Create: `tests/test_closed_loop.py`

**Interfaces:**

- Consumes: Task 12 的 `client_factory`/`db_factory`/`auth_token` 夹具；各服务 app
- Produces: 关键链路自动化回归集（登录、行情、下单+风控、持仓、回测、闭环）

- [ ] **Step 1: tests/test_auth.py**

```python
def test_login_success_and_me(auth_token, client_factory):
    import app.main as seam
    with client_factory(seam) as c:
        resp = c.get("/auth/me")
        assert resp.status_code == 200
        assert resp.json()["username"] == "admin"
        # 无 token 访问受保护端点应 401
        c2 = TestClient(seam.app)  # 不带 header
        c2.dependency_overrides[get_db] = lambda: TestingSessionLocal()
        with c2:
            r = c2.get("/auth/me")
            assert r.status_code == 401
```

> `TestClient`/`get_db`/`TestingSessionLocal` 需在文件顶部按 conftest 的导出导入；为省事，直接在文件内 `from conftest import ...`（conftest 在同一目录会自动 importable）。本计划约定测试文件顶部统一：

```python
import sys, pathlib
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1] / "tests"))
from conftest import TestingSessionLocal, get_db
from fastapi.testclient import TestClient
```

- [ ] **Step 2: tests/test_data_service.py**

```python
def test_stocks_and_quote(client_factory):
    import data_app  # data-service 无同名冲突，用 sys.path 别名 -> from app.main import app as data_app
```

> 各服务模块名都是 `app.main`，在同进程同时 import 会互相覆盖 sys.modules。规避：测试**逐个服务 import 并立即使用**，或给每个 app 文件重命名引用。采用最稳做法：每个测试文件只 import 自己服务对应的 `app.main as X`，且测试进程内**不并发**。`test_data_service.py`：

```python
import app.main as data_app_module

def test_stocks_list(client_factory):
    with client_factory(data_app_module) as c:
        stocks = c.get("/market/stocks")
        assert stocks.status_code == 200
        assert len(stocks.json()) >= 1

def test_quote(client_factory):
    with client_factory(data_app_module) as c:
        q = c.get("/market/quote/600000", params={"provider": "mock"})
        assert q.status_code == 200
        body = q.json()
        assert body["code"] == "600000"
        assert float(body["price"]) > 0

def test_kline(client_factory):
    with client_factory(data_app_module) as c:
        r = c.get("/market/kline/600000", params={
            "start_date": "2024-01-01", "end_date": "2024-01-31", "provider": "mock"})
        assert r.status_code == 200
        assert len(r.json()) > 0
```

> 注意：data-service 的 `get_db` 被 override 为 SQLite 会话，但 market 路由只用 `db` 参数（不写表），mock provider 纯内存，无 redis 也可跑（`RedisCache` 已 try/except）。运行一定不能在这几个测试里同时 import strategy-engine 的 `app.main`。

- [ ] **Step 3: tests/test_trading.py**

```python
import app.main as trade_app_module

def test_place_order_filled(client_factory, monkeypatch):
    """下单 → 风控(桩放行) → 模拟成交成交 → 订单状态 filled"""
    import httpx

    captured = {}

    class FakeRiskResp:
        status_code = 200
        def json(self):
            return {"passed": True, "level": "normal", "message": "ok", "checks": []}

    class FakeRiskClient:
        def __init__(self, *a, **k):
            pass
        async def __aenter__(self):
            return self
        async def __aexit__(self, *a):
            return False
        async def post(self, url, json=None, params=None):
            if "/risk/check" in url:
                return FakeRiskResp()
            return FakeRiskResp()

    from app.engine import trading_engine
    await_init = getattr(trading_engine, "_initialized", False)

    monkeypatch.setattr(httpx, "AsyncClient", FakeRiskClient)

    with client_factory(trade_app_module) as c:
        resp = c.post("/trading/order", json={
            "code": "600000", "direction": "buy", "order_type": "limit",
            "price": 10.0, "quantity": 100,
        })
        assert resp.status_code == 201
        body = resp.json()
        assert body["status"] == "filled"
        assert body["filled_quantity"] == 100

        trades = c.get("/trading/trades").json()
        assert len(trades) == 1
```

> 说明：`submit_order` 内部 `import httpx` 用的是模块内的 names？引擎 `_simulate_fill` 和 `submit_order` 都是函数内 `import httpx`——monkeypatch `httpx.AsyncClient` 作用在 `httpx` 模块属性上，函数内 `import httpx` 拿到同一模块对象，因此替换生效。

- [ ] **Step 4: tests/test_risk_api.py**

```python
import app.main as risk_app_module

def test_risk_check_no_token_passes(client_factory):
    with client_factory(risk_app_module) as c:
        # 覆盖为无 token（conftest client 带 token），单独建无 header 的客户端
        app = risk_app_module.app
        from fastapi.testclient import TestClient
        c2 = TestClient(app)
        c2.dependency_overrides[get_db] = TestingSessionLocal
        with c2:
            resp = c2.post("/risk/check", json={
                "order_no": "O1", "code": "600000", "direction": "buy",
                "price": 10.0, "quantity": 100,
            })
            assert resp.status_code == 200
            body = resp.json()
            assert "passed" in body

def test_risk_limits_crud(client_factory):
    with client_factory(risk_app_module) as c:
        r = c.post("/risk/limits", json={
            "limit_name": "Position 30pct",
            "risk_type": "position_limit",
            "limit_value": 0.3,
            "warning_value": 0.24,
            "description": "position cap",
            "enabled": True,
        })
        assert r.status_code == 201
        limits = c.get("/risk/limits").json()
        assert any(x["limit_name"] == "Position 30pct" for x in limits)
```

- [ ] **Step 5: tests/test_portfolio_api.py**

```python
import app.main as port_app_module

def test_positions_from_process_trade(client_factory):
    with client_factory(port_app_module) as c:
        r = c.post("/portfolio/process-trade", json={
            "code": "600000", "direction": "buy", "price": 10.0, "quantity": 1000,
            "commission": 5.0, "name": "浦发银行", "order_no": "O-TX-1",
        })
        assert r.status_code == 200
        pos = c.get("/portfolio/positions").json()
        assert any(p["code"] == "600000" and p["quantity"] == 1000 for p in pos)

        s = c.get("/portfolio/summary").json()
        assert s["positions_count"] >= 1

def test_deposit(client_factory):
    with client_factory(port_app_module) as c:
        before = c.get("/portfolio/account").json()["balance"]
        c.post("/portfolio/deposit", params={"amount": 10000})
        after = c.get("/portfolio/account").json()["balance"]
        assert after == pytest.approx(before + 10000)
```

> 引擎是模块单例，前一个用例写的数据会影响后一个。计划把 `PortfolioEngine` 增加 `reset()`（用于测试隔离），在 conftest 的 `client_factory` 中，`build()` 每次先调用对应引擎的 `reset()`。R4 实现时在三个引擎类各加：

```python
    def reset(self):
        """重置引擎状态（测试隔离用）"""
        self._positions.clear() if hasattr(self, "_positions") else None
        self._initialized = False
        self._running = False
        if hasattr(self, "_account"):
            self._account = None
        if hasattr(self, "_logs"):
            self._logs.clear()
```

对应 trading/risk `reset()` 重置各自 in-memory 字段。`client_factory` 的 `build()` 在做 `dependency_overrides` 时按 service 名（computed from app title）重置对应引擎。

- [ ] **Step 6: tests/test_backtest_api.py**

```python
import app.main as bt_app_module

def test_backtest_create_and_status(client_factory):
    with client_factory(bt_app_module) as c:
        # 先造一个策略
        strat = c.post("/strategy/", json={
            "name": "BT St", "code": "bt_ma", "type": "dual_ma",
            "description": "bt", "params": {"code": "600000"},
        })
        assert strat.status_code == 201
        sid = strat.json()["id"]
        r = c.post("/backtest/", json={
            "name": "BT1", "strategy_id": sid, "code_list": ["600000"],
            "start_date": "2024-01-01", "end_date": "2024-06-30",
            "initial_capital": 100000, "commission_rate": 0.0003, "slip_rate": 0.001,
        })
        assert r.status_code == 201
        bt_id = r.json()["id"]
        assert r.json()["status"] in ("pending", "running")
        st = c.get(f"/backtest/{bt_id}/status")
        assert st.status_code == 200
```

> 注意：backtest-service 内部 `execute_backtest_task` 会调 `http://data-service:8006`——测试里后台任务可能在客户端关闭时仍在跑，断言只覆盖「创建与状态接口」，不等待结果。

- [ ] **Step 7: tests/test_closed_loop.py（策略→信号→单）

```python
import pytest
import sys
sys.path.insert(0, "services/strategy-engine")

from app.engine.manager import strategy_manager
from strategies.base import BaseStrategy, BarData, SignalAction, StrategyStatus


class LoopStrategy(BaseStrategy):
    name = "loop"
    version = "1.0.0"
    description = "first bar buy"
    def initialize(self, params: dict):
        self._initialized = True
    def on_bar(self, bar: BarData):
        if len(self.get_bar_history(bar.code)) == 1:
            return self.create_signal(bar.code, SignalAction.BUY, bar.close, 100, "loop")
        return None


@pytest.mark.asyncio
async def test_closed_loop_signal_to_order(monkeypatch):
    from app import feed, bridge

    class FakeClient:
        async def get_quote(self, code, provider="mock"):
            return {"code": code, "price": 11.0, "volume": 1234}
    monkeypatch.setattr(feed, "data_client", FakeClient())
    monkeypatch.setattr(bridge, "data_client", FakeClient())

    received = {"signal": None}
    monkeypatch.setattr(bridge, "persist_signal",
                        lambda s, db_factory=None: received.__setitem__("signal", s))
    posted = {"ok": False}
    async def fake_submit(signal):
        posted["ok"] = True
        posted["code"] = signal.code
    monkeypatch.setattr(bridge, "submit_order", fake_submit)

    await strategy_manager.initialize()
    strat = LoopStrategy("S9", {"code": "600000"})
    strat.initialize({"code": "600000"})
    strategy_manager.add_strategy(strat)
    strategy_manager._strategies["S9"]._status = StrategyStatus.RUNNING

    await feed.feed_market_bars()
    await asyncio.sleep(0.05)  # 让异步信号任务落定

    assert received["signal"] is not None
    assert received["signal"].action == SignalAction.BUY
    assert posted["ok"] is True
    assert posted["code"] == "600000"
```

> 本测试不真正 POST 交易服务，验证「feed → 策略出信号 → 回调被触发 → 落库入口被调 → 下单被调」的闭环接线。

- [ ] **Step 8: 全量跑测试**

Run: `py -m pytest tests/ -v`
Expected: PASS（test_boot + test_manager + test_feed + test_bridge + 3 个 persistence + strategy_api + 6 个关键链路 ≈ 30+ passed）

如有个别用例因状态码/枚举差异失败，按实际错误修正断言（保持与 common.exceptions 映射一致）后重跑，并在 commit 说明。

- [ ] **Step 9: 提交**

```bash
git add tests/
git commit -m "test: key-path suites for auth/data/trading/risk/portfolio/backtest/closed-loop"
```

---

### Task 14: docker-compose 手工 E2E 验证清单

**Files:**

- Create: `docs/superpowers/plans/2026-09-14-manual-e2e-checklist.md`（或将下述清单直接写入 README「快速体验」）

**Interfaces:**

- 前置：R6 的 docker-compose env 注入与 healthcheck 已完成；本地有 Docker。

- [ ] **Step 1: 启动全栈**

```powershell
docker compose up -d --build
docker compose ps
```

Expected: 8 个容器 healthy/running（mysql/redis/api-gateway/data/strategy/backtest/trading/portfolio/risk/web）。

- [ ] **Step 2: 登录并取 token**

```powershell
curl.exe -s -X POST http://localhost/api/v1/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'
```

Expected: 返回 `{"user":...,"access_token":{...}}`，记下 access_token。

- [ ] **Step 3: 创建双均线策略并运行**

```powershell
curl.exe -s -X POST http://localhost/api/v1/strategy/ -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" -d '{"name":"双均线","code":"dual_ma_1","type":"dual_ma","params":{"code":"600000","short_period":5,"long_period":20,"position_size":100}}'
curl.exe -s -X POST http://localhost/api/v1/strategy/1/run -H "Authorization: Bearer <TOKEN>"
```

Expected: 创建返回 201，run 返回 `{"status":"running",...}`。

- [ ] **Step 4: 等待 feed 并验证信号落库**

```powershell
# 等 60 秒（2 个 feed 周期）
Start-Sleep -Seconds 60
curl.exe -s "http://localhost/api/v1/strategy/1/signals" -H "Authorization: Bearer <TOKEN>"
```

Expected: 至少 1 条 `buy` 信号（策略列 `strategy_signals` 有行）。

- [ ] **Step 5: 打开 TRADING_ENABLED 复验闭环（可选演练）**

```yaml
# docker-compose: strategy-engine 加 environment:
#   - TRADING_ENABLED=true
#   - TRADING_MODE=simulation
```

重建后等 2-3 个 feed 周期，检查：

```powershell
curl.exe -s http://localhost/api/v1/trading/orders -H "Authorization: Bearer <TOKEN>"
curl.exe -s http://localhost/api/v1/portfolio/summary -H "Authorization: Bearer <TOKEN>"
curl.exe -s http://localhost/api/v1/portfolio/positions -H "Authorization: Bearer <TOKEN>"
```

Expected: orders 出现 `filled`；portfolio positions 出现 600000 持仓；MySQL 中 `orders/trades/positions/accounts/strategy_signals` 表有数据。

- [ ] **Step 6: 记录结果到陈列出**

将实际输出写入 `docs/manual-e2e-results-2026-09-14.md`，作为闭环验证证据。

- [ ] **Step 7: 提交**

```bash
git add docs/manual-e2e-results-2026-09-14.md
git commit -m "docs: manual E2E closed-loop verification results"
```

---

## R5 前端体验完善

### Task 15: 抽取图表/表格/指标卡片组件 + useWebSocket

**Files:**

- Create: `web-frontend/src/components/MetricCard.vue`
- Create: `web-frontend/src/components/EquityCurveChart.vue`
- Create: `web-frontend/src/components/PositionsTable.vue`
- Create: `web-frontend/src/components/StockChart.vue`
- Create: `web-frontend/src/composables/useWebSocket.js`

**Interfaces:**

- Consumes: `echarts`（已在依赖）、`element-plus` 表格组件
- Produces: 全局可复用组件（props/events 见各文件代码），Dashboard 等视图使用

- [ ] **Step 1: 写 MetricCard**

`web-frontend/src/components/MetricCard.vue`：

```vue
<template>
  <el-card shadow="hover" class="metric-card" v-loading="loading">
    <div class="metric-label">{{ label }}</div>
    <div class="metric-value" :class="valueClass">
      {{ prefix }}{{ fmtValue }}
    </div>
    <div v-if="sub" class="metric-sub" :class="valueClass">{{ sub }}</div>
  </el-card>
</template>

<script setup>
import { computed } from "vue";

const props = defineProps({
  label: { type: String, required: true },
  value: { type: [Number, String], default: 0 },
  prefix: { type: String, default: "¥ " },
  sub: { type: String, default: "" },
  positive: { type: Boolean, default: true },
  loading: { type: Boolean, default: false },
});

const fmtValue = computed(() => {
  const n = Number(props.value) || 0;
  return n.toLocaleString("zh-CN", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  });
});
const valueClass = computed(() => (props.positive ? "" : "is-negative"));
</script>

<style scoped>
.metric-label {
  color: #909399;
  font-size: 13px;
}
.metric-value {
  font-size: 24px;
  font-weight: bold;
  margin-top: 8px;
}
.metric-sub {
  font-size: 13px;
  margin-top: 4px;
}
.is-negative {
  color: #f56c6c;
}
</style>
```

- [ ] **Step 2: 写 EquityCurveChart + StockChart**

`web-frontend/src/components/EquityCurveChart.vue`（权益/回撤双轴，空态提示）：

```vue
<template>
  <div v-loading="loading" class="chart-box">
    <div v-if="empty" class="chart-empty">暂无数据</div>
    <div v-else ref="chartEl" style="height: 320px"></div>
  </div>
</template>

<script setup>
import { ref, onMounted, onBeforeUnmount, watch, nextTick } from "vue";
import * as echarts from "echarts";

const props = defineProps({
  data: { type: Array, default: () => [] }, // [{date, equity}]
  loading: { type: Boolean, default: false },
});
const chartEl = ref(null);
let chart = null;
const empty = computed(() => !props.data || props.data.length === 0);
import { computed } from "vue";

const render = () => {
  if (!chartEl.value || empty.value) return;
  chart = chart || echarts.init(chartEl.value);
  chart.setOption({
    tooltip: { trigger: "axis" },
    grid: { left: 60, right: 20, top: 20, bottom: 40 },
    xAxis: { type: "category", data: props.data.map((d) => d.date) },
    yAxis: { type: "value", scale: true },
    series: [
      {
        name: "权益",
        type: "line",
        smooth: true,
        areaStyle: { opacity: 0.12 },
        data: props.data.map((d) => d.equity),
        itemStyle: { color: "#409eff" },
      },
    ],
  });
};

const resize = () => chart && chart.resize();

onMounted(async () => {
  await nextTick();
  render();
  window.addEventListener("resize", resize);
});
onBeforeUnmount(() => {
  window.removeEventListener("resize", resize);
  chart && chart.dispose();
});
watch(
  () => props.data,
  () => {
    nextTick(render);
  },
);
</script>

<style scoped>
.chart-box {
  position: relative;
}
.chart-empty {
  height: 320px;
  line-height: 320px;
  text-align: center;
  color: #909399;
}
</style>
```

`web-frontend/src/components/StockChart.vue`（K 线 + 成交量，接收 OHLC 数组）：

```vue
<template>
  <div v-loading="loading" class="chart-box">
    <div v-if="empty" class="chart-empty">暂无行情数据</div>
    <div v-else ref="chartEl" style="height: 360px"></div>
  </div>
</template>

<script setup>
import {
  ref,
  computed,
  onMounted,
  onBeforeUnmount,
  watch,
  nextTick,
} from "vue";
import * as echarts from "echarts";

const props = defineProps({
  code: { type: String, default: "" },
  klines: { type: Array, default: () => [] }, // [{date, open, high, low, close, volume}]
  loading: { type: Boolean, default: false },
});
const chartEl = ref(null);
let chart = null;
const empty = computed(() => !props.klines || props.klines.length === 0);

const render = () => {
  if (!chartEl.value || empty.value) return;
  chart = chart || echarts.init(chartEl.value);
  chart.setOption({
    tooltip: { trigger: "axis", axisPointer: { type: "cross" } },
    grid: [
      { left: 60, right: 20, top: 20, height: "62%" },
      { left: 60, right: 20, top: "74%", height: "18%" },
    ],
    xAxis: [
      {
        type: "category",
        data: props.klines.map((k) => k.date),
        boundaryGap: false,
      },
      {
        type: "category",
        data: props.klines.map((k) => k.date),
        boundaryGap: false,
        gridIndex: 1,
        axisLabel: { show: false },
      },
    ],
    yAxis: [{ scale: true }, { gridIndex: 1, scale: true }],
    dataZoom: [{ type: "inside", xAxisIndex: [0, 1] }],
    series: [
      {
        name: "K线",
        type: "candlestick",
        data: props.klines.map((k) => [k.open, k.close, k.low, k.high]),
        itemStyle: { color: "#f56c6c", color0: "#67c23a" },
      },
      {
        name: "量",
        type: "bar",
        xAxisIndex: 1,
        yAxisIndex: 1,
        data: props.klines.map((k) => k.volume),
      },
    ],
  });
};
const resize = () => chart && chart.resize();
onMounted(async () => {
  await nextTick();
  render();
  window.addEventListener("resize", resize);
});
onBeforeUnmount(() => {
  window.removeEventListener("resize", resize);
  chart && chart.dispose();
});
watch(
  () => props.klines,
  () => nextTick(render),
);
</script>
<style scoped>
.chart-box {
  position: relative;
}
.chart-empty {
  height: 360px;
  line-height: 360px;
  text-align: center;
  color: #909399;
}
</style>
```

- [ ] **Step 3: 写 PositionsTable**

`web-frontend/src/components/PositionsTable.vue`：

```vue
<template>
  <div v-loading="loading">
    <el-empty v-if="empty" description="暂无持仓" :image-size="80" />
    <el-table v-else :data="positions" size="small" max-height="320">
      <el-table-column prop="code" label="代码" width="90" />
      <el-table-column prop="name" label="名称" width="100" />
      <el-table-column prop="quantity" label="数量" width="80" align="right" />
      <el-table-column label="市值" align="right">
        <template #default="{ row }">{{ fmt(row.market_value) }}</template>
      </el-table-column>
      <el-table-column label="浮动盈亏" align="right">
        <template #default="{ row }">
          <span :class="row.unrealized_pnl >= 0 ? 'profit' : 'loss'">{{
            fmt(row.unrealized_pnl)
          }}</span>
        </template>
      </el-table-column>
      <el-table-column label="盈亏%">
        <template #default="{ row }">
          <span :class="row.unrealized_pnl_pct >= 0 ? 'profit' : 'loss'">{{
            pct(row.unrealized_pnl_pct)
          }}</span>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<script setup>
import { computed } from "vue";
defineProps({
  positions: { type: Array, default: () => [] },
  loading: { type: Boolean, default: false },
});
const empty = computed(() => !props.positions || props.positions.length === 0);
const fmt = (v) =>
  (Number(v) || 0).toLocaleString("zh-CN", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  });
const pct = (v) => ((Number(v) || 0) * 100).toFixed(2) + "%";
</script>

<style scoped>
.profit {
  color: #67c23a;
}
.loss {
  color: #f56c6c;
}
</style>
```

> 若 `props` 未在 `defineProps` 中解构用于 script，需在脚本内 `const props = defineProps({...})`（如上，`empty` 引用 props）。上面的 `defineProps` 直接无赋值导致 `props` 未定义——正确写法为 `const props = defineProps({...})`。**实现时必须用 `const props = defineProps({...})`。**

- [ ] **Step 4: 写 useWebSocket**

`web-frontend/src/composables/useWebSocket.js`：

```javascript
import { ref, onBeforeUnmount } from "vue";

export function useWebSocket(channel, { onMessage } = {}) {
  const connected = ref(false);
  let ws = null;
  let retryTimer = null;

  const url = () => {
    const proto = window.location.protocol === "https:" ? "wss:" : "ws:";
    const host = window.location.host;
    return `${proto}//${host}/ws/${channel}`;
  };

  const connect = () => {
    ws = new WebSocket(url());
    ws.onopen = () => {
      connected.value = true;
    };
    ws.onmessage = (e) => {
      let data = e.data;
      try {
        data = JSON.parse(e.data);
      } catch (_) {
        /* plain text */
      }
      if (data === "pong") return;
      onMessage && onMessage(data);
    };
    ws.onclose = () => {
      connected.value = false;
      retryTimer = setTimeout(connect, 5000);
    };
    ws.onerror = () => {
      ws && ws.close();
    };
  };

  const send = (payload) => {
    if (ws && ws.readyState === WebSocket.OPEN)
      ws.send(JSON.stringify(payload));
  };
  const close = () => {
    clearTimeout(retryTimer);
    ws && ws.close();
    ws = null;
  };

  connect();
  onBeforeUnmount(close);
  return { connected, send, close };
}
```

- [ ] **Step 5: `npm run build` 验证新组件可编译**

Run（在 `web-frontend`）:

```powershell
npm install
npm run build
```

Expected: `vite build` 成功，`dist/` 生成，无语法错误（新组件未引用处不会触发，但 Babel/Vue 编译会校验每个 .vue）。

- [ ] **Step 6: 提交**

```bash
git add web-frontend/src/components web-frontend/src/composables
git commit -m "feat(web): reusable chart/table/card components + websocket composable"
```

---

### Task 16: Dashboard 仪表盘重构（加载/空态/实时刷新）

**Files:**

- Modify: `web-frontend/src/views/Dashboard.vue`

**Interfaces:**

- Consumes: Task 15 组件、`useWebSocket`、`portfolioAPI/riskAPI`
- Produces: 仪表盘四卡片 + 权益曲线 + 持仓表格 + 组合分析 + 实时刷新开关

- [ ] **Step 1: 重写 Dashboard.vue**

```vue
<template>
  <div class="dashboard">
    <el-row :gutter="20">
      <el-col :span="6"
        ><MetricCard
          label="总资产"
          :value="summary.total_assets"
          :loading="loading"
      /></el-col>
      <el-col :span="6"
        ><MetricCard
          label="持仓市值"
          :value="summary.positions_value"
          :loading="loading"
      /></el-col>
      <el-col :span="6"
        ><MetricCard label="可用资金" :value="summary.cash" :loading="loading"
      /></el-col>
      <el-col :span="6">
        <MetricCard
          label="总收益"
          :value="summary.total_profit"
          :positive="summary.total_profit >= 0"
          :sub="pct(summary.total_profit_pct)"
          :loading="loading"
        />
      </el-col>
    </el-row>

    <el-row :gutter="20" style="margin-top: 20px">
      <el-col :span="16">
        <el-card shadow="hover">
          <template #header>
            <div class="card-header">
              <span>权益曲线</span>
              <el-switch v-model="realtime" active-text="实时刷新" />
            </div>
          </template>
          <EquityCurveChart :data="equity" :loading="loading" />
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card shadow="hover">
          <template #header><span>持仓概览</span></template>
          <PositionsTable :positions="positions" :loading="loading" />
        </el-card>
      </el-col>
    </el-row>

    <el-row :gutter="20" style="margin-top: 20px">
      <el-col :span="12">
        <el-card shadow="hover">
          <template #header><span>风险预警</span></template>
          <el-table :data="alerts" size="small" max-height="260">
            <el-table-column prop="code" label="代码" width="90" />
            <el-table-column prop="message" label="消息" />
            <el-table-column prop="level" label="等级" width="90">
              <template #default="{ row }">
                <el-tag :type="levelType(row.level)" size="small">{{
                  row.level
                }}</el-tag>
              </template>
            </el-table-column>
          </el-table>
        </el-card>
      </el-col>
      <el-col :span="12">
        <el-card shadow="hover">
          <template #header><span>组合分析</span></template>
          <div v-loading="loading">
            <el-empty
              v-if="!positions.length"
              description="暂无持仓数据"
              :image-size="80"
            />
            <div v-else ref="pieChart" style="height: 260px"></div>
          </div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup>
import { ref, onMounted, nextTick, computed, onBeforeUnmount } from "vue";
import * as echarts from "echarts";
import MetricCard from "../components/MetricCard.vue";
import EquityCurveChart from "../components/EquityCurveChart.vue";
import PositionsTable from "../components/PositionsTable.vue";
import { useWebSocket } from "../composables/useWebSocket";
import { portfolioAPI, riskAPI } from "../api";

const loading = ref(false);
const realtime = ref(false);
const summary = ref({
  total_assets: 0,
  positions_value: 0,
  cash: 0,
  total_profit: 0,
  total_profit_pct: 0,
});
const positions = ref([]);
const alerts = ref([]);
const equity = ref([
  { date: new Date().toISOString().slice(0, 10), equity: 0 },
]);
const pieChart = ref(null);
let pChart = null;

const fmt = (v) =>
  (Number(v) || 0).toLocaleString("zh-CN", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  });
const pct = (v) => ((Number(v) || 0) * 100).toFixed(2) + "%";
const levelType = (lvl) =>
  ["danger", "blocked"].includes(lvl)
    ? "danger"
    : lvl === "warning"
      ? "warning"
      : "info";

const loadData = async () => {
  loading.value = true;
  try {
    const [sumRes, posRes, alertRes] = await Promise.all([
      portfolioAPI.getSummary(),
      portfolioAPI.getPositions(),
      riskAPI.getAlerts(),
    ]);
    summary.value = sumRes.data;
    positions.value = posRes.data || [];
    alerts.value = alertRes.data || [];
    const today = new Date().toISOString().slice(0, 10);
    equity.value = [{ date: today, equity: sumRes.data.total_assets || 0 }];
    await nextTick();
    renderPie();
  } finally {
    loading.value = false;
  }
};

const renderPie = () => {
  if (!pieChart.value || !positions.value.length) return;
  pChart = pChart || echarts.init(pieChart.value);
  pChart.setOption({
    tooltip: { trigger: "item" },
    legend: { bottom: 0 },
    series: [
      {
        type: "pie",
        radius: ["40%", "70%"],
        data: positions.value
          .slice(0, 8)
          .map((p) => ({ name: p.code, value: p.market_value || 0 })),
      },
    ],
  });
};

// 实时刷新：WebSocket 推送 portfolio.update 时刷数据
const { connected } = useWebSocket("portfolio.update", {
  onMessage: () => {
    if (realtime.value) loadData();
  },
});
const refreshTimer = ref(null);
watch(realtime, (v) => {
  if (v && !refreshTimer.value)
    refreshTimer.value = setInterval(loadData, 15000);
  if (!v && refreshTimer.value) {
    clearInterval(refreshTimer.value);
    refreshTimer.value = null;
  }
});

import { watch } from "vue";
onMounted(loadData);
onBeforeUnmount(() => {
  refreshTimer.value && clearInterval(refreshTimer.value);
});
</script>

<style scoped>
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
</style>
```

> `import { watch } from 'vue'` 合并到顶部 import；`watch(realtime,...)` 逻辑把 15 秒轮询作为 WS 之外的兜底。

- [ ] **Step 2: `npm run build` 验证**

Run（在 `web-frontend`）: `npm run build`
Expected: 构建成功，无编译错误。

- [ ] **Step 3: 提交**

```bash
git add web-frontend/src/views/Dashboard.vue
git commit -m "feat(web): dashboard uses shared components with loading/empty/realtime"
```

---

## R6 治理与收敛

### Task 17: requirements 收敛 + common/requirements.txt 落地（修 CI）

**Files:**

- Create: `services/common/requirements.txt`
- Modify: 6 个 `services/*/requirements.txt`

**Interfaces:**

- Consumes: `.github/workflows/ci.yml` 已引用 `services/common/requirements.txt` 与 `tests/requirements-test.txt`
- Produces: 统一基线（见 Global Constraints），任一服务 `pip install -r` 可运行

- [ ] **Step 1: 建 common 基线**

创建 `services/common/requirements.txt`：

```
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
sqlalchemy==2.0.25
pymysql==1.1.0
redis==5.0.1
python-dotenv==1.0.0
httpx==0.26.0
psutil==5.9.8
prometheus-fastapi-instrumentator==6.1.0
apscheduler==3.10.4
```

- [ ] **Step 2: 更新各服务 requirements 为「基线 + 个性依赖」**

- `services/data-service/requirements.txt`：加入 `pydantic-settings==2.1.0`、`psutil==5.9.8`（其余已具备：tushare/apscheduler/sqlalchemy/pymysql/redis/dotenv）。
- `services/trading-service/requirements.txt` 改为：

```
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
sqlalchemy==2.0.25
pymysql==1.1.0
redis==5.0.1
python-dotenv==1.0.0
httpx==0.26.0
psutil==5.9.8
prometheus-fastapi-instrumentator==6.1.0
```

- `services/portfolio-service/requirements.txt`：在现有 sqlalchemy/pymysql/redis 基础上补 `pydantic-settings==2.1.0`、`python-dotenv==1.0.0`、`psutil==5.9.8`。
- `services/risk-service/requirements.txt`：补 `sqlalchemy==2.0.25`、`pymysql==1.1.0`、`redis==5.0.1`、`pydantic-settings==2.1.0`、`python-dotenv==1.0.0`、`psutil==5.9.8`（保留 numpy/apscheduler）。
- `services/strategy-engine/requirements.txt`：补 `sqlalchemy==2.0.25`、`pymysql==1.1.0`、`redis==5.0.1`、`pydantic-settings==2.1.0`、`python-dotenv==1.0.0`、`psutil==5.9.8`（保留 numpy/pandas/apscheduler）。
- `services/backtest-service/requirements.txt`：补 `pydantic-settings==2.1.0`、`python-dotenv==1.0.0`、`redis==5.0.1`、`psutil==5.9.8`（保留 sqlalchemy/pymysql）。

- [ ] **Step 3: 验证安装**

```powershell
py -m pip install -r services/common/requirements.txt -r tests/requirements-test.txt
```

Expected: 安装完成，无 `ERROR: No matching distribution`（版本需存在/可解析；若有冲突以本机已装为准）。

- [ ] **Step 4: 提交**

```bash
git add services/common/requirements.txt services/*/requirements.txt
git commit -m "chore: unify dependency baseline, add common requirements for CI"
```

---

### Task 18: docker-compose 环境注入 + healthcheck（dev 可一键起）

**Files:**

- Modify: `docker-compose.yml`

**Interfaces:**

- Consumes: `SECURITY_SECRET_KEY` env、mysql/redis healthcheck
- Produces: `docker compose up -d --build` 后全部服务可用、健康检查绿

**背景：** dev compose 未注入密钥（各服务默认 32 字符 dev key 已够用，但显式注入更稳）；无 healthcheck，服务可能在被依赖项就绪前启动。

- [ ] **Step 1: 改造 docker-compose.yml**

在 mysql/redis 上加 healthcheck；每个服务加 `environment` 注入密钥并 `depends_on` 就绪条件；补 `trading-service`、`strategy-engine`、`backtest-service`、`risk-service` 对 mysql 的依赖（read 持久化）：

```yaml
version: "3.8"

x-security: &security
  SECURITY_SECRET_KEY: ${SECURITY_SECRET_KEY:-dev-secret-key-quant-system-2026}
  SECURITY_ACCESS_TOKEN_EXPIRE_MINUTES: 30
  SECURITY_REFRESH_TOKEN_EXPIRE_DAYS: 7

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: quant123
      MYSQL_DATABASE: quant_system
    ports: ["3306:3306"]
    volumes: [mysql_data:/var/lib/mysql]
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-pquant123"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  api-gateway:
    image: nginx:alpine
    ports: ["80:80"]
    volumes: ["./nginx.conf:/etc/nginx/nginx.conf:ro"]
    depends_on:
      data-service: { condition: service_started }
      strategy-engine: { condition: service_started }
      backtest-service: { condition: service_started }
      trading-service: { condition: service_started }
      portfolio-service: { condition: service_started }
      risk-service: { condition: service_started }

  data-service:
    build: ./services/data-service
    environment: *security
    ports: ["8006:8006"]
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }

  strategy-engine:
    build: ./services/strategy-engine
    environment: *security
    ports: ["8001:8001"]
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }
      data-service: { condition: service_started }

  backtest-service:
    build: ./services/backtest-service
    environment: *security
    ports: ["8002:8002"]
    depends_on:
      mysql: { condition: service_healthy }

  trading-service:
    build: ./services/trading-service
    environment: *security
    ports: ["8003:8003"]
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }

  portfolio-service:
    build: ./services/portfolio-service
    environment: *security
    ports: ["8004:8004"]
    depends_on:
      mysql: { condition: service_healthy }

  risk-service:
    build: ./services/risk-service
    environment: *security
    ports: ["8005:8005"]
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }

  web-frontend:
    build: ./web-frontend
    ports: ["3000:3000"]
    depends_on:
      api-gateway: { condition: service_started }

volumes:
  mysql_data:
```

- [ ] **Step 2: 本地校验 yaml**

```powershell
docker compose config
```

Expected: `docker-compose.yml` 解析通过，无 schema 报错。

- [ ] **Step 3: 提交**

```bash
git add docker-compose.yml
git commit -m "chore(docker): inject SECURITY env, add healthchecks and mysql deps"
```

---

### Task 19: README 快速体验文档（闭环 How-to）

**Files:**

- Modify: `README.md`

- [ ] **Step 1: 在 README 追加「快速体验（本地闭环）」小节**

```markdown
## 快速体验（本地闭环）

### 1. 一键起全栈

docker compose up -d --build

### 2. 登录（默认 admin/admin123）

curl -s -X POST http://localhost/api/v1/auth/login \
-H "Content-Type: application/json" \
-d '{"username":"admin","password":"admin123"}'

# 记下返回 access_token.token

### 3. 创建并运行双均线策略

curl -s -X POST http://localhost/api/v1/strategy/ \
-H "Authorization: Bearer <TOKEN>" \
-H "Content-Type: application/json" \
-d '{"name":"双均线","code":"dual_ma_1","type":"dual_ma","params":{"code":"600000","short_period":5,"long_period":20}}'

curl -s -X POST http://localhost/api/v1/strategy/1/run -H "Authorization: Bearer <TOKEN>"

### 4. 等待 60s 后查看信号（feed_bars 每 30s 喂一根 bar）

curl -s "http://localhost/api/v1/strategy/1/signals" -H "Authorization: Bearer <TOKEN>"

### 5. 查看交易 / 持仓 / 风控 / 组合

curl -s http://localhost/api/v1/trading/orders -H "Authorization: Bearer <TOKEN>"
curl -s http://localhost/api/v1/portfolio/summary -H "Authorization: Bearer <TOKEN>"
curl -s http://localhost/api/v1/portfolio/positions -H "Authorization: Bearer <TOKEN>"
curl -s http://localhost/api/v1/risk/checks -H "Authorization: Bearer <TOKEN>"

> 开启自动下单：在 docker-compose 的 strategy-engine 服务加
> environment: - TRADING_ENABLED=true - TRADING_MODE=simulation
> 后重建 `docker compose up -d --build strategy-engine`。
```

- [ ] **Step 2: 校验 markdown**

Run: `py -m pytest tests/ -v`（回归全绿）并确 README 无语法破坏。
Expected: 全绿。

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "docs: add quickstart for local closed-loop experience"
```

---

## Self-Review

**1. Spec coverage（对应 design 文档六节）**

- 稳定运行修复 → Task 1-5
- 闭环打通 → Task 6-8
- 持久化 → Task 9-11（信号落库在 Task 8，回测表已存在验证在 Task 12/13）
- 关键链路测试 → Task 12-14（自动化 + 手工 E2E 双轨）
- 前端体验 → Task 15-16
- 治理收敛 → Task 17-19
- 范围外标注：6 个 Dockerfile.prod、monitoring/prometheus、grafana、init_db.sql、SSL → 见 Global Constraints「本计划不包含」。

**2. Placeholder 扫描**：所有任务均含可执行代码/命令；`test_strategy_api` 的 duplicate 状态码以「400 或 422」二选一并对实现后实际结果确认（可执行语义，非空占位）。Task 13 的 `reset()` 已在 conftest/引擎实现给出具体代码。

**3. 类型一致性检查**

- `Save_order/trade/rebuild_engine(db_factory=SessionLocal)` 签名在 Task 9 定义，测试一致使用。
- `bridge.handle_signal`/`register_handlers` 名称在 Task 8 定义，`test_closed_loop` 引用一致。
- `feed.feed_market_bars`/`fetch_bar` 名称 Task 7 定义，Task 8 注册、Task 13 测试引用一致。
- `persist_signal(signal, db_factory=...)` 在 bridge 定义，closed-loop 测试按该签名 monkeypatch。
- BarData 字段（to_dict → from_dict）两侧对称；`process_bars` 改「直连处理」后 Task 6 测试断言单次触发。
